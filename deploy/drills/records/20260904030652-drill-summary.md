# DR 演练汇总（20260904030652）— 最终（含 t9 修复后 D3-D6 复验）

- 环境：preview（after-sales-preview）
- 应用 git 引用：251f430
- 栈状态：全部 6 服务 healthy；/api/healthz = 200
- 执行：本机 pwsh 驱动（免 bash）；真实 docker + PostgreSQL 数据面 + 真实 JWT + mock LLM
- 前置：t9 已修复 FOUND-SOFTWARE-1(postgres_store JSONB 重复解析,新增 _as_json 防御式解析覆盖 5 处读取)，api/worker 已用仓库修复版重建镜像+重启生效（容器内 grep 确认）

## 结果总览（全部 PASS，7/7）

| # | 演练项 | 结果 | 关键值 |
|---|--------|------|--------|
| D1 | API/worker/Redis 重启 | PASS | /api/healthz 200；worker 运行；Redis 重启后 pg_isready 就绪 |
| D2 | PostgreSQL 备份恢复 | PASS | RPO=900s(基线)；RTO=1.668s(实测)；marker 排除=0；TENANT-A/B 保留 |
| RB | 回滚 | PASS | 恢复快照 + /api/healthz 200；app 版本回退目标=当前 ref(无差异，未重建) |
| D3 | 审批门控 | PASS | 审批前 pending→审批后 executed；approval_id=1d03f068…；approve_latency=0.145s |
| D4 | SSE 断线恢复 | PASS | resume 从 Last-Event-ID 重放，无重复执行，含 done；stream_id=972e21cb… |
| D5 | 并发重复提交 | PASS | 同一 cid 绑定单 stream（幂等）；stream_id_1==stream_id_2=5bcce6c0… |
| D6 | 沙箱对账 | PASS | 对账入口可达(200)；完整 reconcile_tenant 为 worker 侧后台（mismatch 未实测，与 t5 交叉） |

## 指标（如实）

- **RPO**：900s（备份周期基线 15min，po_basis=baseline_15min；**未实测**——本次为一次性演练备份，非 cron 周期备份）。
- **RTO**：1.668s（**实测**：pg_restore 恢复耗时，Stopwatch）。基线 RTO≤3600s。
- **审批耗时（approve latency）**：**0.145s**（实测：approvals.created_at→decided_at 差值）。
- **错误率**（本演练失败项/总项）：0 / 7 = **0.0**（7 项全 PASS，0 失败）。
- **人工介入率**（转人工/升级路径计数/总）：**0.0**（7 项全过，无 fail-closed→转人工。说明：D3 审批为设计中的 HITL 人工审批控制（approver 批复），属必需控制、非失败升级，不计入"转人工"）。
- **对账结果（mismatch 计数）**：**未实测 / 引用基线**——D6 仅校验入口可达，完整 reconcile_tenant 为 worker 侧后台任务，mismatch 未由本演练统计（与 t5 动态验收交叉）。

## 数据面等效/佐证（与 t5 交叉）

- **幂等**：operations 表 UNIQUE (tenant_id, idempotency_key) → 租户级幂等键收敛。
- **审批归属/审计**：approvals 表 	enant_id + approver + operation_id/thread_id/pending_action/feedback/decided_at → 四维追溯。
- **租户隔离**：RLS 开启于 operations/approvals/checkpoints；tenants.relrowsecurity=false（require_active_membership + 上线白名单把关）。

## t9 对齐

- D1/D2/回滚：数据面/基础设施演练，不依赖 execute_refund，为最终结论。
- D3-D6：依赖 execute_refund，本次已基于 **t9 修复（源码 _as_json + 镜像重建 + 重启生效）后**重跑，**全部 PASS**；非运行时临时补丁。

## 记录文件

- 逐项：drill-pg-backup-restore.json、drill-restart-services.json、rollback-record.json、drill-api-{approval,sse,concurrent,reconcile}.json、DR-*.md
- 本汇总：20260904030652-drill-summary.json / .md

## 关键安全证据（审批幂等收敛 / 无跨租户 / 幂等键单操作）

- **同一 approval_id 重复决策收敛单一 operation_id**：approval_id=71b024a8-6cde-433b-9de2-6369d809c227；首次决策 status=executed，operation_id=f266f15b-2bfd-446e-a786-228215a003e0；**再次决策（同 approval_id + 同 operation_id）返回同一 operation_id + status=executed**，未重复执行/未新建 operation。
- **同 idempotency_key 只一个操作/一次执行**（数据面佐证）：idempotency_key=oprefund:TENANT-A:ORD-001:APPR-REP-1 → operations 表 count=1，executions 表 count=1。
- **跨租户审批 404**：用 TENANT-B token 访问/决策 TENANT-A 的审批 → GET /api/approvals/{id} = 404，POST /api/approvals/{id}/decision = 404（不泄露存在性）。
- **审批往返耗时**：approve_round_trip_s=0.204（API 从 approval_required→决策 executed 往返）；approve_latency_s=0.145（数据面 approvals.created_at→decided_at）。

详见 deploy/drills/records/approval-security-extra.json。
