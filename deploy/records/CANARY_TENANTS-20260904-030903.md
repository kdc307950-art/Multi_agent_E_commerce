# 灰度租户清单（正式交付 · 基于 t1/t2/t5/t6 真实结果回填）

> t4 模板回填为正式交付物。首批租户与放量阶梯基于 **t1 部署+门控配置、t2 静态合规、t5 动态安全验收**；放量须由
> `SCALEUP_APPROVAL-20260904-030903.md` 批准。**跨租户默认拒绝**；仅同租户 `admin`/`approver` 可审批。
> 生成时间戳：`20260904-030903`。

> **口径更新说明（t5 接线后，superseded 归档）**：本记录为 t1 早期交付状态，其中「`seed_default` 预置 TENANT-A/B 各 4 角色、`AUTH_LOGIN_CREDENTIALS` 用 sha256 hex、键集与 seed_default 10 成员一致」的表述**已被新基线取代**——
> `preview/production` 一律**禁用 `seed_default`**（`DEMO_SEED_ENABLED=false`），首批真实租户与成员由**受控迁移/运维脚本**创建，登录凭据必须为 **argon2id/bcrypt PHC**（`AUTH_CREDENTIAL_HASH`，禁止 sha256/md5）。此处保留历史事实仅作归档，不作为当前验收依据。

---

## 0. 记录标识
- 记录 ID：`CANARY-20260904-030903`
- 维护人 / 复核人：`deploy-engineer` / `security-auditor`
- 依据：`LAUNCH_ALLOWED_TENANTS=TENANT-A,TENANT-B`（t1）+ `src/main.py::seed_default` 预置 + `AUTH_LOGIN_CREDENTIALS`（t1）+ `verify_launch_gate --strict`（t5）

## 1. 首批上线租户白名单（= 上线门控允许集合）
| 租户 ID | 是否在 `LAUNCH_ALLOWED_TENANTS` | 数据面预置（seed_default） | 状态/准入 | 依据 |
|---------|-------------------------------|--------------------------|-----------|------|
| `TENANT-A` | 是（`launch_allowlist=[TENANT-A,TENANT-B]`） | 是 | 启用；`tenant_allowed(TENANT-A)=True` | t1/t5 |
| `TENANT-B` | 是 | 是 | 启用 | t1/t5 |
| `<EVIL-TENANT>` | **否** | — | **拒绝**（非名单租户强制拒，受限环境） | t5（`tenant_allowed(EVIL-TENANT)=False`） |
| 其它候选 | 未纳入首批 | — | 需另行审批 | — |

> 说明：`seed_default` 预置 TENANT-A / TENANT-B 各含 customer/agent/admin/approver 成员；`AUTH_LOGIN_CREDENTIALS`
> 键集与 seed_default 的 **10 个成员完全一致**（t1 实测），可为这些成员签发真实 JWT。

## 2. 成员角色与归属（服务端 `TenantContext` 认证后取，禁止信任客户端）
> 角色仅限 `customer`/`agent`/`admin`/`approver`；`platform_admin` 为独立受控能力，不作租户成员角色（t2 RL-03）。
> 登录凭据来自 `AUTH_LOGIN_CREDENTIALS`（`{"<tenant_id>:<user_id>": "<sha256hex>"}`），t1 已为 10 个成员生成。

| 租户 | 成员（角色集合） | 可审批（admin/approver） | 凭据注入 |
|------|-----------------|--------------------------|----------|
| `TENANT-A` | `customer` / `agent` / `admin` / `approver` | 是（admin、approver） | 已注入（匹配 seed_default 10 成员） |
| `TENANT-B` | `customer` / `agent` / `admin` / `approver` | 是（admin、approver） | 已注入 |

> 具体 user_id 与明文映射存 `deploy/secrets/preview_login_credentials.txt`（gitignored，**不明文回显**）。t5 已实测：
> `customer` 越权审批 → **403**；非目标租户审批 → **404**；跨租户读取 → **404**。

## 3. 放量阶梯（渐进放量设计）
| 阶梯 | 租户范围 | 放量动作 | 通过条件 | 审批单引用 |
|------|---------|----------|---------|-----------|
| Tier 0（内部验收） | `TENANT-A` | 首个租户 | `healthcheck` 4 项全过 + DR 演练可复核 + RPO≤15min/RTO≤60min | `SCALEUP_APPROVAL-20260904-030903.md` |
| Tier 1（灰度） | `TENANT-A` + `TENANT-B` | 扩到 2 租户 | 阈值全部 + 无重复副作用 + 指标正常 | 待放量审批 |
| Tier 2 / 3 | `<后续区间>` | 逐步/全量 | 告警无异常 + 二次审批 | 待审 |
- 当前所处方队：`Tier 0-1 边界`（首批 2 租户已就绪；是否放量由`SCALEUP_APPROVAL`决定）

## 4. 放量审批单引用
- 本次放量审批单：`deploy/records/SCALEUP_APPROVAL-20260904-030903.md` —— **已生成（本 t7 汇总）**，结论见其文件。
- 放量前条件（阈值+演练可复核）：见`SCALEUP_APPROVAL`§1；其中**RPO=900s 达标（t12 15min 周期备份界定）、RTO=1.668s 实测达标**、D3-D6 **t10 复跑全部 PASS（t9 修复后）**；**仍未实测**：对账 mismatch、多租户并发、应用侧 trace、告警指标增强。审批结论需据此判定。

## 5. 风险与备注
| 风险 | 结论 | 依据 |
|------|------|------|
| 跨租户访问 | 默认拒绝；跨租户审批/读取 404；RLS FORCE + `app_runtime NOBYPASSRLS` | t2 RL-03 / t5 §1 |
| 停用租户/成员失效 | `require_active_membership` + 上线白名单把关（`tenants.relrowsecurity=false` 由该守卫补位） | t2 RL-02 / t6 数据面证据 |
| 记忆/知识/向量/图谱按租户隔离 | 本阶段 **非前置**（Graphiti/Neo4j/Milvus 未启用；检索走 keyword 确定性） | t2/PREVIEW §9 |
| 角色/审批归属 | 仅 admin/approver 可审批；同租户；二次确认 422 强制；跨租户 404 | t5 §1/§2 |
| **FOUND-SOFTWARE-1（执行路径崩溃）** | 审批通过后执行收尾/SSE 重放/`/api/audit` 曾受 JSONB 重复解析影响；**t9 已修复并 commit（HEAD=`7941246`，`_as_json` 5 处）+ 重建生效 + E2E 通过** | t5 §4 / t9（`SOFTWARE_FIX_FOUND_1.md`） |

## 结论（如实）
- 首批租户（TENANT-A/B）是否就绪、成员角色正确、数据面预置：**是**（t1/t5）
- 是否允许进入 Tier 0/1：**需谨慎（继续等待或按观察项放量）**。放量前条件中 **RPO≤15min 已达标（t12 15min 周期备份界定）、告警指标未增强（待办）、对账 mismatch 后台未实测**；**宿主仅 80/443 已达成（t8）、langfuse 观测已生效（t8）、D3-D6 已基于 t9 修复后（HEAD=7941246）t10 复跑全部 PASS、FOUND-SOFTWARE-1 已修复、RTO 达标、无跨租户/重复/绕过/未审计写（动态验收）均已达成**。
- 签发：`deploy-engineer` / `security-auditor`
