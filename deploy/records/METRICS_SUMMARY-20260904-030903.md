# 指标汇总表（正式交付 · 注明实测/周期界定/未实测）

> 汇总 t1/t5/t6/t8/t9/t10/t12 的真实口径核心指标。**如实区分实测、周期界定与未实测；未实测项一律标注，不声称达标。**
> 生成时间戳：`20260904-030903`（终稿·t13）。来源：t1=部署、t2=静态评审、t5=动态安全验收、t6=DR 演练、t8=观测/宿主端口、t9=FOUND-SOFTWARE-1 修复、t10=D3-D6 复跑、t12=15min 周期备份。

## 核心指标表

| 指标 | 值 | 单位 | 实测 / 基线 / 待回填 | 来源/依据 | 达标阈值 | 达标？ |
|------|----|------|----------------------|-----------|---------|--------|
| **RPO** | 900（=15min） | s | **周期界定**（15min 定时备份周期界定：实测 marker_in_restore=0、TENANT_A_in_restore=1、restore_RTO=1.9s） | t12：`docker-compose.preview.yml` `backup` 服务每 900s `pg_dump -Fc` + 保留 8 + 宿主持久 `./data/preview-backups`（首份 `langgraph-20260903193257.dump`） | ≤ 900s | ✅ **达标（周期界定）**（RPO 上界=15min 周期备份；PITR/WAL 为更紧 RPO 的后续增强） |
| **RTO** | 1.668 | s | **实测**（Stopwatch 计时） | t6 `rto_restore_seconds=1.668` | ≤ 3600s | ✅ **实测达标** |
| **错误率** | 0.0 | 比例 | **实测（本演练，7/7 全 PASS）** | t10/t6 `drill-summary.json`：`error_rate=0`（0/7） | — | ✅（基于演练项） |
| **错误率（生产 API）** | 无值 | — | **无法计量**（缺 `status` 标签） | `metrics.py`/`routes.py`：`api_requests_total` 仅 (route,method) | — | ⚠️ 需指标增强 |
| **人工介入率** | 0.0 | 比例 | **实测（本演练，7/7 全 PASS）** | `drill-summary`：`human_intervention_rate=0`；D3 审批为设计 HITL 控制（approver 批复），非失败升级 | — | ✅（基于演练项） |
| **审批耗时** | **0.145**（数据面 `created_at→decided_at`）/ **0.204**（API 往返 `approval_required→executed`） | s | **实测** | t10 `drill-api-approval.json`（approve_latency=0.145s）+ `drill-summary`（approve_round_trip_s=0.204） | — | ✅（实测；无阈值定义） |
| **对账结果（mismatch 计数）** | 未实测 | count | **未实测/待后台**（D6 仅校验入口可达+审计；完整 reconcile_tenant 为 worker 后台，与 t5 交叉） | t10 `drill-api-reconcile.json`；t6 旧 `reconcile_mismatch=null` 已更正 | ≠0 即告警 | ⚠️ mismatch 待后台任务 |
| **无跨租户** | PASS | — | **实测**（t5 + t9 源码修复重建后） | t5 `sec_dynamic_acceptance.md` §1 | 允许=0 | ✅ |
| **无重复执行** | PASS | — | **实测**（t5/t9 `executions` 落库 `GROUP BY HAVING count>1=0`） | t5/t9 | 重复=0 | ✅ |
| **无审批绕过** | PASS | — | **实测**（pending→approved→executed；越权 403；缺二次确认 422；跨租户 404） | t5/t9 | 绕过=0 | ✅ |
| **无未审计写操作** | PASS | — | **实测**（decision/execution 均 append_audit；拒绝路径 security.deny.*） | t5/t9 | 未审计写=0 | ✅ |
| **D3 审批门控** | PASS | — | **实测**（ap approval→executed） | t10 `drill-api-approval.json` | — | ✅ |
| **D4 SSE 恢复** | PASS | — | **实测**（resume 含 done，无重复执行） | t10 `drill-api-sse.json` | — | ✅ |
| **D5 并发幂等** | PASS | — | **实测**（同 cid→stream_id_1==stream_id_2） | t10 `drill-api-concurrent.json` | — | ✅ |
| **D6 沙箱对账** | PASS（入口） | — | **部分实测**（入口 200+审计） | t10 `drill-api-reconcile.json` | — | ⚠️ mismatch 待后台 |

## 指标口径与诚实说明

1. **RPO 周期界定达标**：t12 已加入 **15min 周期 pg_dump 备份**（backup 服务每 900s，保留 8，宿主持久 `./data/preview-backups`，首份 `langgraph-20260903193257.dump`），并跑「备份→写标记→恢复临时库→校验」：**marker_in_restore=0、TENANT_A_in_restore=1、restore_RTO=1.9s**。故 **RPO 上界=15min 备份周期界定**（非硬编码基线），满足 RPO≤15min。**边界**：pg_dump 周期而非 PITR/WAL 归档（PITR/WAL 为更紧 RPO 的后续增强项）。
2. **RTO 实测达标**：1.668s（`pg_restore` 恢复耗时，Stopwatch）≤3600s。
3. **错误率/人工介入率为「本演练项」口径**（t10 复跑 7/7 全 PASS、0 失败；D3 审批的 HITL 人工批复为必需控制，不计为「转人工」）。**不能当作生产运行错误率/人工介入率**（后者缺 `status`/`human_intervention_total` 指标，暂无法计量）。
4. **审批耗时 0.145s 为实测**（t10），但无阈值定义；宜在放量后设置运行期 p95 阈值（需新增 `approval_decision_latency_seconds` 直方图）。
5. **对账 mismatch 计数未实测**：D6 仅校验对账入口可达 + 审计存在；完整 `reconcile_tenant` 为 worker 侧后台任务，mismatch 未由本演练统计（与 t5 交叉）。
6. **FOUND-SOFTWARE-1**：t9 已修复并 **commit（HEAD=`7941246`）**——`src/infrastructure/postgres_store.py` 新增 `_as_json`（5 处 JSONB 读取切换，`+20/-5`，未夹带 migrations.py），重建 api/worker 生效，E2E 通过（`executed`、SSE、audit 均正常）。前一提交 `251f430` 为缺陷版；**`migrations.py` 为先前会话改动仍未提交**。
7. **指标增强缺口（告警前提）**：`api_requests_total` 缺 `status`；无 `approval_decision_latency_*`/`reconcile_mismatch_total`/`human_intervention_total`/`drill_{rpo,rto}_seconds`/`security_denials_total`。需在 `src/observability/metrics.py`+`src/api/routes.py` 增加**有界标签**指标（禁用高基数 `tenant_id` 等），`deploy/observability/alert-rules.yml` 相关告警方会真正生效。

## 参考证据文件
- t6：`deploy/drills/records/20260904030652-drill-summary.json/.md`、`drill-pg-backup-restore.json`、`drill-restart-services.json`、`rollback-record.json`
- t10：`deploy/drills/records/drill-api-{approval,sse,concurrent,reconcile}.json`
- t5：`evidence/sec_dynamic_acceptance-20260904-030405.json/.md`；t9：`evidence/SOFTWARE_FIX_FOUND_1.md`
- t2：`evidence/sec_static_review.json/.md`；观测：`deploy/observability/metrics.py`、`src/api/routes.py`
