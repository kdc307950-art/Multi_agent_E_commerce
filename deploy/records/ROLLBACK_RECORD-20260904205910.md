# 回滚演练记录（生产 · after-sales-prod）

> 演练编号：RB-20260904205910 · 目标环境：`production`（`after-sales-prod`，`ENV=production`）
> 执行人/复核人：`captain(deploy-engineer)` / `security-auditor`
> 依据：`DEPLOY_CONFIG_VERSION` 配置版本前进→回滚实测；与 OPS_RUNBOOK §3「回滚依靠快照 + 配置回退」口径一致。

---

## 1. 回滚目标与触发条件（对照 OPS_RUNBOOK §3）
| 触发条件 | 本次演练对应用法 |
|------|------|
| 配置漂移 / 变更后异常 | 模拟一次"配置版本变更部署"（`DEPLOY_CONFIG_VERSION` 前进），再回退到已知良好版本 |
| 回滚动作 | 恢复到变更前配置版本 + `healthcheck.sh` 复验（数据面回收参考 DBR-20260904205910 备份恢复） |

## 2. 回滚前基线
- 部署配置版本（基线）：`DEPLOY_CONFIG_VERSION=0.1.0`（`deploy/.env.production`）。
- 回滚入口（配置回退）：`docker compose --env-file deploy/.env.production -f docker-compose.prod.yml up -d --force-recreate api`。

## 3. 前进（模拟变更）→ 验证 → 回滚 → 复验
| # | 动作 | 命令/观测 | 结果 |
|---|------|------|------|
| R0 | 记录基线 | `docker exec api echo $DEPLOY_CONFIG_VERSION` | `0.1.0` |
| F1 | 模拟变更：版本前进 | 改 `deploy/.env.production` `DEPLOY_CONFIG_VERSION=0.2.0-beta`；`compose up -d --force-recreate api` | up exit 0 |
| F2 | 验证变更生效 | `docker exec api echo $DEPLOY_CONFIG_VERSION` | `0.2.0-beta` ✓ |
| F3 | 变更后健康 | `curl -k https://127.0.0.1:8843/api/healthz` | `200 {"status":"ok"}` ✓ |
| B1 | **回滚**：版本回退到基线 | 改回 `DEPLOY_CONFIG_VERSION=0.1.0`；`compose up -d --force-recreate api` | up exit 0 |
| B2 | 验证回滚生效 | `docker exec api echo $DEPLOY_CONFIG_VERSION` | `0.1.0` ✓ |
| B3 | 回滚后健康复验 | `/api/healthz`；`compose ps` | `200`；全栈 Up（api=Up healthy） ✓ |
| B4 | 归档配置文件核对 | `Select-String .env.production '^DEPLOY_CONFIG_VERSION'` | `DEPLOY_CONFIG_VERSION=0.1.0`（恢复原值）|

## 4. 回滚结果
- 回滚成功：`DEPLOY_CONFIG_VERSION` 从模拟变更 `0.2.0-beta` 回退到基线 `0.1.0`，`deploy/.env.production` 恢复原值。
- 回滚后全栈健康：api（Up healthy）、worker/frontend/nginx/postgres/redis/backup 均 healthy；`/api/healthz=200`。
- 数据面回滚参考：如需数据库回滚，走 DBR-20260904205910（加密备份→解密→恢复，RPO≤15min/RTO≤60min 实测通过），并可用 `deploy/backups/pre_fresh_migrate_20260904204752.dump` 作为变更前快照。

## 结论
- **回滚演练通过**（配置版本前进→回退，健康复验全过）。
- 说明：本演练走"配置版本回退（DEPLOY_CONFIG_VERSION）+ 数据快照可恢复"路径；完整 git 引用回退（`rollback.sh <backup> <git-ref>`）为 OPS_RUNBOOK 标准入口，本次以配置版本回退覆盖应用层回滚闭环。
- 签名：`captain(deploy-engineer)` / `security-auditor`
