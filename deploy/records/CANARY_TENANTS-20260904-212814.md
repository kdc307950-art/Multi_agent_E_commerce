# 灰度租户清单（正式确认函 · 模板/示例 · 待用户确认）

> **状态：`[AWAITING_USER_CONFIRMATION]`（待真实租户名单并提供真实成员/角色）**。本记录为 **t2（infra）** 依据
> `deploy/records/tenants.json`（示例）与 t1 复盘结论生成的可交付确认函框架。**真实租户名单系外部输入（BLOCKED_EXTERNAL）**，
> 未经业务方/用户书面确认前，**不得**把示例租户写入生产 `LAUNCH_ALLOWED_TENANTS` 或 `AUTH_LOGIN_CREDENTIALS`。
> 生成时间戳：`20260904-212814`。

> **前提红线**：`preview/production` 一律**禁用 seed_default**（`DEMO_SEED_ENABLED=false`）。首批真实租户与成员
> 必须由**受控迁移/运维脚本**（`scripts/create_bootstrapped_tenants.py`）创建，登录凭据必须为 **argon2id/bcrypt PHC**
> （`AUTH_CREDENTIAL_HASH`，禁 sha256/md5）。**角色仅限** `customer`/`agent`/`admin`/`approver`；
> `platform_admin` 为独立平台能力，**不是**租户成员角色。

---

## 0. 记录标识
- 记录 ID：`CANARY-20260904-212814`
- 生成时间戳 `<ts>`：`20260904-212814`
- 维护人 / 复核人：`infra（t2 后端/基础设施） / security（t3 安全合规）`
- 依据：
  - `tenants.json`（`deploy/records/tenants.json`，**示例**，已通过 `create_bootstrapped_tenants.py --dry-run` 校验）
  - `LAUNCH_ALLOWED_TENANTS`（生产）当前 **`__NONE_APPROVED_YET__`**（fail-closed）
  - `AUTH_LOGIN_CREDENTIALS`（生产）当前 **`{}`**（fail-closed，无人可登录）
  - `AUTH_CREDENTIAL_HASH=argon2id`（生产）

## 1. 首批上线租户白名单（= 上线门控允许集合）
> 生产 **`LAUNCH_ALLOWED_TENANTS=__NONE_APPROVED_YET__`**（未确认任何真实租户 → 门控 fail-closed，拒绝全部）。
> 下表为**示例**（`tenants.json` 中示例租户），仅供参考；**未确认前一律视为"待用户确认 / 未知"**。

| 租户 ID | 是否在 `LAUNCH_ALLOWED_TENANTS` | 数据面是否已由受控脚本创建 | 状态/准入 | 回填来源 |
|---------|-------------------------------|--------------------------|-----------|---------|
| `ACME-RETAIL`（**示例**） | 否——生产 `__NONE_APPROVED_YET__` | 否（未执行） | `待确认/未知` | t2（示例）、**待用户确认** |
| `GLOBEX-ECOM`（**示例**） | 否 | 否 | `待确认/未知` | t2（示例）、**待用户确认** |
| `<真实租户-1>` | `<是/否>` | `<是/否>` | `<...>` | **用户/业务方提供**、t2/t3 |
| `<真实租户-2>` | `<...>` | `<...>` | `<...>` | **用户/业务方提供**、t2/t3 |

> **说明**：`LAUNCH_ALLOWED_TENANTS` 不为空才放行；真实租户确认后由 release-manager 写入，例如
> `LAUNCH_ALLOWED_TENANTS=<real-tenant-id-1>,<real-tenant-id-2>`，并经 `verify_launch_gate --strict` 复核。

## 2. 成员角色与归属（服务端 `TenantContext` 认证后取，禁止信任客户端）
> 角色仅限 `customer`/`agent`/`admin`/`approver`；`platform_admin` 不作租户成员角色。
> 登录凭据来自 `AUTH_LOGIN_CREDENTIALS`（JSON：`{"<tenant_id>:<user_id>": "<PHC 哈希（argon2id|bcrypt）>"}`），
> 由 t2/t3 核验；`AUTH_CREDENTIAL_HASH` 仅允许 `argon2id|bcrypt`。下表为 `tenants.json` 的**示例**成员与角色。

| 租户 | 成员 user_id | 角色 | 是否可审批（admin/approver） | 凭据注入 |
|------|-------------|------|------------------------------|----------|
| `ACME-RETAIL`（示例） | `acme-admin-1` | `admin` | **是** | 未注入（示例；生产 `AUTH_LOGIN_CREDENTIALS={}`） |
| `ACME-RETAIL`（示例） | `acme-approver-1` | `approver` | **是** | 未注入 |
| `ACME-RETAIL`（示例） | `acme-agent-1` | `agent` | 否 | 未注入 |
| `ACME-RETAIL`（示例） | `acme-customer-1` | `customer` | 否 | 未注入 |
| `GLOBEX-ECOM`（示例） | `globex-admin-1` | `admin` | **是** | 未注入 |
| `GLOBEX-ECOM`（示例） | `globex-approver-1` | `approver` | **是** | 未注入 |
| `GLOBEX-ECOM`（示例） | `globex-agent-1` | `agent` | 否 | 未注入 |
| `GLOBEX-ECOM`（示例） | `globex-customer-1` | `customer` | 否 | 未注入 |

> **凭据注入路径（确认真实租户后）**：
> 1. 用 `scripts/hash_login_credentials.py --algorithm argon2id --creds-env <ENV>`（明文不经磁盘）生成 PHC JSON；
> 2. 将产物注入生产 `deploy/.env.production` 的 `AUTH_LOGIN_CREDENTIALS`；确认 `AUTH_CREDENTIAL_HASH=argon2id`；
> 3. **绝不打印/落盘明文**；产物只含 `<tenant>:<user>` → PHC 哈希。明文只在 `--creds-env` / `getpass` 一时输入。

## 3. 放量阶梯（渐进上线设计）
| 阶梯 | 租户范围 | 放量动作 | 通过条件（阈值+演练） | 审批单引用 |
|------|---------|----------|----------------------|-----------|
| Tier 0（内部验收） | 首个真实租户（最小面） | 仅启用 1 个租户 | `healthcheck` 全过 + DR 演练可复核 + RPO≤15min/RTO≤60min | `SCALEUP_APPROVAL-<ts>` |
| Tier 1（灰度） | 首个 + 第 2 个租户 | 扩到 2 个租户 | 上线阈值全部 + 无重复副作用 + 指标正常 | `<...>` |
| Tier 2（放量 50%） | `<目标租户区间>` | 逐步扩量 | 告警无异常 + 人工介入率/错误率达标 | `<...>` |
| Tier 3（全量） | `<全部目标租户>` | 全量 | 稳定期观察 + 二次审批 | `<...>` |
- **当前所处阶梯**：`等待真实租户确认（Tier 0 前置）`；放量依据《OPS_RUNBOOK §2 上线阈值》《§3 回滚条件》。

## 4. 放量审批单引用
- 本阶梯放量审批单：`deploy/records/SCALEUP_APPROVAL-<ts>.md` —— `待创建`（t7 汇总）
- 放量前条件（阈值达成 + 演练记录可复核）：`待 t3/t6 演练确认`（见《OPS_RUNBOOK §2》）
- 审批人（租户 `admin`/`approver`）：`待真实租户 + 真实成员角色确认`；复核人：`security` —— **同租户审批，禁止跨租户**

## 5. 风险与备注
- 跨租户风险：`待 t3 安全评审回填`（跨租户访问默认拒绝；数据面 RLS + `TenantScopedCheckpointer`；审批/操作须同租户）
- 停用租户 / 成员失效：`待 t3 确认`（停用即阻断新请求与后台任务；`require_active_membership` 把关）
- 记忆/知识/向量/图谱按租户隔离：`待 t3 回填`（Graphiti/Milvus 本阶段非前置；检索走 keyword 确定性）
- 放量/停用须留痕（操作者、审批依据、时间、结果）：`t2/t3/t7`

## 结论（待用户确认，勿臆造）
- 首批真实租户是否已就绪、成员角色正确：**否**（**真实租户名单尚未由业务方/用户提供**；`tenants.json` 仅为示例）
- 是否允许进入 Tier 0/1：**否**（须真实租户书面确认 + `SCALEUP_APPROVAL` 审批；示例租户一律不得放量）
- 签发：`infra（t2） / security（t3 复核）`
