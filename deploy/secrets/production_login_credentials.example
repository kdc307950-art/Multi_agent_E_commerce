# deploy/secrets/production_login_credentials.example -- 生产登录凭据格式模板（**不含任何真实明文**）。
#
# 运行时不从此文件读取明文；真实明文登录凭据在运行时由操作者/密钥管理注入（见下）。
# 服务端 `AUTH_LOGIN_CREDENTIALS` 只保存 PHC 哈希（Argon2id/bcrypt，默认为 argon2id），
# 生成方式（生产必须 argon2id，禁 sha256/md5）：
#   python scripts/hash_login_credentials.py --algorithm argon2id --creds-env <ENV>  （推荐，明文不经磁盘）
#   python scripts/hash_login_credentials.py --algorithm argon2id                      （stdin 输入，Ctrl+D 结束）
#
# 字段格式（每行 <tenant_id>:<user_id> = <明文口令>，仅用于**说明**；**切勿填写真实值**）：
#   <tenant_id>:<user_id> = <PLACEHOLDER -- 运行时由操作者注入，绝不写入本文件>
#
# 产出的 JSON 形如（下面仅演示结构，占位哈希非真实口令结果，仅为格式示例）：
#   {"ACME-RETAIL:acme-admin-1": "$argon2id$v=19$m=65536,t=3,p=4$...", ...}
#
# 安全红线：
#   - 不硬编码、不读明文文件、不把明文写进脚本/产物；明文只作为一时输入。
#   - 产物（stdout 或 --output 文件）只含 `<tenant>:<user>` -> PHC 哈希，绝不含明文。
#   - 真实租户确认前，生产 `deploy/.env.production` 的 `AUTH_LOGIN_CREDENTIALS={}` 保持 fail-closed，
#     登录一律拒绝并审计。确认真实租户/成员后由 release-manager 注入。
