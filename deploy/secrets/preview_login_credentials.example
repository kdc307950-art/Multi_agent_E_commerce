# deploy/secrets/preview_login_credentials.example -- 格式模板（**不含任何明文**）。
#
# 运行时不从此文件读取明文；明文登录凭据在运行时由操作者/密钥管理注入（见下）。
# 服务端 AUTH_LOGIN_CREDENTIALS 只保存 PHC 哈希（Argon2id/bcrypt，默认为 argon2id），
# 生成方式：python scripts/hash_login_credentials.py --algorithm argon2id（或 bcrypt）。
#
# 字段格式（每行 <tenant_id>:<user_id> = <明文口令>，仅用于**说明**；**切勿填写真实值**）：
#   <tenant_id>:<user_id> = <PLACEHOLDER -- 运行时由操作者注入，绝不写入本文件>
#
# 运行时的明文注入方式（deploy/drills/pwsh-runbook/*.py）：
#   1) 环境变量 PREVIEW_LOGIN_CREDENTIALS（JSON 对象）：{"<tenant_id>:<user_id>": "<明文>", ...}
#   2) 未提供环境变量时，脚本交互式用 getpass 提示输入（无回显，不落日志）。
#
# 安全红线：不硬编码、不读明文文件、不把明文写进脚本或产物；明文只作为一时输入。
