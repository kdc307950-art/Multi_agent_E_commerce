# 电商售后多智能体工单系统 — 生产环境架构设计（Production Architecture Design）

> **定位**：本文件描述**生产级部署架构**（前后端分离 · 分层解耦），与《项目架构说明书》的 **Agent 内部流程架构**互补——前者回答"系统怎么部署/扩展/运维"，后者回答"Agent 内部怎么编排/流转"。
> **核心结论**：生产目标**跳过 Streamlit/Chainlit 原型**，采用 **React + Next.js 专业前端 + FastAPI 网关 + LangGraph/CrewAI 核心 + 高可用 PostgreSQL/Neo4j/Milvus 生产拓扑 + 自托管可观测** 的分层解耦架构。`Milvus standalone` 只用于本地和预发布验证，不能代表生产高可用。系统可以部署在项目自管机房或项目账户下租赁的 IaaS 服务器；“完全自托管”指 LLM、数据、可观测与运维由项目方自运维，**不依赖公有 SaaS**，而非要求数据永远停留在单台本机。每个请求先解析服务端 `TenantContext`，再进入 Agent 与数据面。

---

## 0. 为什么生产不能停留在"能用"

| 痛点 | 说明 | 应对 |
|---|---|---|
| **性能与扩展性** | 高并发下仅对话优化不够 | 专业前端 + 后端独立扩容 |
| **深度定制与品牌化** | 面向客户/内部员工需统一品牌视觉 | React/Next.js 高度定制 |
| **企业级功能集成** | 需权限、审计日志、数据看板深度集成 | 成熟前端框架更易实现 |
| **数据可控** | 数据留在项目方控制边界内 | 全自托管 LLM/向量/图库/可观测 |

> Chainlit / Streamlit **定位**：适合内部演示、原型、快速验证；**不作为生产对外界面**。

---

## 1. 生产架构总览（分层解耦）

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│  用户端 → DNS / TLS / 自建 Ingress 或负载均衡                                   │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  ① 前端展示层：React + Next.js 多副本；Chainlit / Streamlit 仅内部演示          │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  ② 接入层：FastAPI API 多副本 + SSE；服务端 TenantContext、RBAC、限流与审计     │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  ③ 核心服务层：LangGraph / CrewAI / MCP / 审批；Celery worker 多副本            │
│     敏感写操作只在审批后由具备租户作用域的 worker 执行                           │
└───────────────────────────────────┬──────────────────────────────────────────┘
                                    ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  ④ 私有数据面：PostgreSQL HA · Neo4j 生产恢复/集群方案 · Milvus distributed    │
│     Redis HA · 自托管 Langfuse · Prometheus/Grafana · 备份与恢复演练             │
└──────────────────────────────────────────────────────────────────────────────┘
```

**核心思想：前后端分离，各层可独立扩展、维护、演进**。

---

## 2. 各层设计与选型

### 2.1 前端展示层（React + Next.js）

| 选项 | 说明 | 建议 |
|---|---|---|
| **React** | 生态最大、资源最丰富，适合复杂企业应用；配 **Next.js** 元框架 | 选它 |
| **UI 组件库** | **Ant Design**（开箱即用，适合后台管理） | AntD |
| **AI 交互组件** | **Vercel AI SDK**（流式渲染标准库） | 构建聊天界面 |

> 本项目建议：**React + Next.js + Ant Design + Vercel AI SDK**。注意 `Vercel AI SDK` 只是前端库，**不要求部署到 Vercel 平台**。若使用 Next.js SSR/Route Handler，需自建 Next.js runtime（或 standalone output）并由 Nginx/Ingress 反向代理；只有明确采用静态导出时，才可只用 Nginx/内网 CDN 托管构建产物。

### 2.2 API 网关与接入层（FastAPI）

- **REST API + SSE**：作为后端统一出口；SSE 承载 AI 流式输出。
- **自动生成 OpenAPI 文档**：前后端协作友好。
- 性能高、支持异步，是 Python 后端 API 事实标准。
- **租户入口守卫**：认证后从服务端令牌/成员关系解析 `TenantContext`，校验租户状态和角色；不接受客户端提供的 `tenant_id`，无上下文或跨租户资源请求默认拒绝。

> **协议收敛**：统一主协议为 **SSE（单一 `POST /api/chat`）**；**不混用 WebSocket/GraphQL 双址**，保持前端接入一致。WebSocket 仅作可选扩展（需要双向即时交互时）。

### 2.3 核心服务层（LangGraph + CrewAI + MCP）

- 主控 LangGraph + CrewAI 子智能体 + 工具/记忆/Skill——即《项目架构说明书》所述内部流程。
- 生产上在此层接入 **人工审批（interrupt）**、**Graphiti + Neo4j 记忆层**（见《记忆架构设计》）。
- `tenant_id` 作为不可变状态贯穿主图、子智能体、RAG、工具和后台任务；任一节点缺少作用域即失败/转人工，不能回退至默认租户。

### 2.4 数据与基础设施层

| 组件 | 用途 |
|---|---|
| **PostgreSQL** | 业务数据、工单、用户、审计记录、检查点；`tenant_id` 联合约束 + RLS/等效 guard；生产采用主备、自动/受控切换与异地备份演练 |
| **Neo4j + Graphiti** | 长期记忆时序知识图谱；节点/关系/事实均带 `tenant_id`；生产可用性方案必须在版本和许可确认后冻结，社区版单实例不能被表述为图数据库 HA |
| **Milvus** | Agentic RAG 向量检索；开发/预发布使用固定版本 standalone Compose，生产使用 distributed/cluster 拓扑；按租户 collection/partition/filter 隔离 |
| **Redis + Celery** | 锁、缓存、队列；key/payload 含 `tenant_id`，消费端再次校验租户状态；生产使用 Redis HA 与多个 worker 副本 |
| **自托管 Langfuse + Prometheus/Grafana** | 全链路可观测；trace/audit 带 `tenant_id`，Prometheus 避免高基数租户标签；告警、日志保留和监控自身备份纳入运维 |

---

## 3. 关键集成：AI 交互（前端 ↔ 后端流式）

**冻结的设计契约**：AI 流唯一入口是 **`POST /api/chat`**，响应始终为 `text/event-stream`；不提供 `/ws/chat`、JSON `ChatResponse` 或第二条 AI 消息入口。本节是接口实现和验收的唯一口径，当前仍是**设计态**，尚未由可运行服务验证。

### 3.1 请求与恢复语义

`POST /api/chat` 的 JSON body 只能是以下两种之一；认证身份和 `tenant_id` 均由服务端 `TenantContext` 解析，客户端不得传入或覆盖。

    {"mode":"start","thread_id":"<server-issued UUID>","client_request_id":"<UUID>","message":"<text>"}
    {"mode":"resume","stream_id":"<opaque stream id>"}

- `mode=start` 必须同时提供 `thread_id`、`client_request_id` 和 `message`。服务端先校验会话归属，再以 `(tenant_id, user_id, client_request_id)` 原子去重；同一请求 ID 重投不得新建第二个图执行或敏感操作。
- `mode=resume` 的 body 只携带 `stream_id`，并且必须在请求头带 `Last-Event-ID: <last received SSE id>`。服务端再次校验该流、会话和游标都属于当前 `TenantContext`，从游标之后重放持久化事件并继续订阅该**既有**流。
- 每个流在发出前持久化其事件 ID 和 data；事件保留期与检查点保留策略一致（当前设计基线为 7 天）。游标不存在、超出保留期或不属于该租户时只返回 `error`，绝不从 `thread_id` 重新执行图。

### 3.2 SSE 帧与事件字典

每个 SSE 帧都必须同时包含 `id`、`event` 与单行 JSON `data`，其中 `id` 在一个 `stream_id` 内严格递增且可作为 `Last-Event-ID` 原样回传。禁止透传 LangGraph 的内部事件，禁止新增 `ping`、`message` 或匿名 `data:` 帧。

    id: 17
    event: accepted
    data: {"stream_id":"s_...","thread_id":"...","client_request_id":"...","mode":"start"}

| 事件 | JSON data 最小字段 | 语义 |
|---|---|---|
| `accepted` | `stream_id, thread_id, client_request_id, mode` | 该请求已绑定到唯一流；前端必须先持久化 `stream_id` 再渲染后续事件。 |
| `token` | `delta` | 增量文本；前端以 `(stream_id, id)` 去重渲染。 |
| `node` | `name, status, progress?` | 可展示的图节点进度，不泄露内部提示词或 PII。 |
| `approval_required` | `approval_id, operation_id, status` | 已生成待审批业务请求；尚未执行敏感写。 |
| `done` | `status, final_response?, operation_id?` | 流正常终止；此后该流不再产生事件。 |
| `error` | `code, message, retryable, operation_id?` | 流失败或无法恢复；是否可恢复由服务端语义决定，不能据此重放敏感写。 |

### 3.3 前端接入与服务端实现边界

该协议是 **POST + 自定义 SSE 事件字典**。浏览器侧应使用 `fetch` 的流式读取配合 SSE parser，或为所选 SDK 编写协议适配器/自定义 transport；**不得假定 Vercel AI SDK 默认 `useChat` 能直接解析上述事件**。`EventSource` 也不适用于此入口，因为其不能提交规定的 POST body。

FastAPI 实现必须将 `ChatStart`/`ChatResume` 做成可判别请求模型，在写入 `accepted` 事件和流映射后才启动或附着执行。LangGraph 事件仅在服务端映射为本节的六种事件；若使用 `astream_events`，`version` 参数只支持 `v1`（已弃用）与 `v2`，**不存在 `v3`**，本项目统一使用 `version="v2"`，并已在本地 `langgraph==1.2.11` 下完成导入与最小运行验收。

### 3.4 断线、取消与敏感写安全

- **读类恢复**：网络断开后，客户端以 `mode=resume`、`stream_id` 与 `Last-Event-ID` 续传；不能用 `thread_id` 或原 `message` 开启替代性重放。
- **敏感写安全**：退款、退货、改址等动作在 `approval_required` 后仅能通过审批 REST 决策。断线、刷新、超时或客户端取消都不得自动重新提交 `mode=start`、审批决定或工具调用；恢复连接本身只重放既有事件，不执行任何业务副作用。
- **结果确认**：一旦获得 `operation_id`，前端只通过 `GET /api/operations/{operation_id}` 查询权威状态。未收到响应时应先查询 `GET /api/approvals/{approval_id}` 获取关联操作，而不是盲目重复写请求。
- **取消与背压**：`AbortController` 只断开客户端读取；服务端按状态机决定安全取消或人工交接。服务端按连接限流、持久化游标和消费者速度实施背压，不能依赖浏览器缓存无限积压。

### 3.5 统一 REST 资源

除上述 AI 流外，面向前端的 REST 统一使用 `/api` 前缀，并在每个资源入口执行服务端身份、角色和租户归属校验：

| 方法与路径 | 用途 |
|---|---|
| `POST /api/sessions` | 创建会话，服务端生成 `thread_id`。 |
| `GET /api/sessions` | 当前主体可见的会话列表。 |
| `GET /api/sessions/{thread_id}`、`GET /api/sessions/{thread_id}/messages` | 会话元数据、摘要和历史消息。 |
| `GET /api/approvals`、`GET /api/approvals/{approval_id}` | 当前主体有权查看的审批列表与详情（含关联 `operation_id`）。 |
| `POST /api/approvals/{approval_id}/decision` | 审批决定；服务端原子处理并返回/关联唯一 `operation_id`。 |
| `GET /api/operations/{operation_id}` | 查询敏感操作的权威状态与可展示结果。 |

人工升级由后端领域服务创建并通过上述会话、审批或操作资源呈现；不暴露第二个 `/escalate` 公共入口。本 REST 表同样是设计态，实际状态码、错误码、鉴权和 OpenAPI 定义需与后续契约测试一起验收。

### 3.1 多租户运行边界

- **PostgreSQL**：所有租户业务表使用 `tenant_id`，以联合唯一约束和 RLS/等效 repository guard 双重隔离；连接池在事务内设置已验证租户作用域。
- **Milvus**：检索和写入必须指定当前租户 collection/partition 或 scalar filter；缓存键包含 `tenant_id`，禁止全局召回兜底。
- **Neo4j/Graphiti**：节点、关系、episode 与 Cypher 查询都先按 `tenant_id` 限定；同名 `user_id`/`order_id` 不得跨租户关联。
- **Redis/Celery**：锁使用 `lock:{tenant_id}:{thread_id}`；任务 payload 携带租户和请求 ID，worker 取到任务后复核租户仍为 active。
- **观测与导出**：trace/审计带 `tenant_id`，而统计指标仅使用有界标签；跨租户查询仅限受审计的独立 `PlatformOperationContext` + `platform_admin` 流程，不能把平台能力作为租户角色或普通对话上下文使用。

---

## 4. 部署策略（前后端独立部署 · 完全自托管）

### 4.1 三层运行拓扑

| 环境 | 目的 | 运行方式 | 明确边界 |
|---|---|---|---|
| **本地开发/功能验证** | 联调、单元/集成测试 | 固定版本 Docker Compose；Milvus `standalone + etcd + MinIO` | 单机、无 HA，不作为生产容量或可用性依据 |
| **线上预发布验证** | 在租赁服务器上验证真实网络、部署、隔离、恢复和压测 | 项目账户下的 IaaS VM 或小型 K8s；前端、API、worker、数据面均自部署 | 是上线前验证环境，不因“部署到公网”自动获得生产 HA |
| **生产目标** | 对外稳定服务 | 多可用节点 K8s/等效编排、Ingress/LB、私有网络、数据服务 HA、监控告警和灾备 | 仅在 §4.2 与《生产基线与验收测试》门槛完成后才可称生产就绪 |

> 租赁 IaaS 服务器可以作为完全自托管的承载环境：项目方负责镜像、密钥、网络、数据库、模型和运维；但需要将数据驻留区域、访问控制、备份位置和供应商责任边界写入上线清单，不能简单沿用“数据不出本地”的字面表述。

### 4.2 生产目标的关键韧性要求

| 区域 | 生产目标 | 不能省略的验证 |
|---|---|---|
| 接入与应用 | 至少 2 个 API 副本和 2 个 worker 副本，经 Ingress/LB 暴露；SSE 重连、限流、请求超时受控 | 关闭一个 API/worker 后，读请求恢复、在途敏感写不重复执行且可人工接管 |
| PostgreSQL 与检查点 | 主备、备份、恢复演练；完整 `TenantScopedCheckpointer` 对读、写、列举、删除、invoke、恢复、历史和审批恢复均校验可信作用域，并在 saver 实际连接内设置 RLS 租户变量 | 恢复后同一 `thread_id` 不能被其他租户读取、恢复或残留在删除任务中 |
| 向量与图谱 | Milvus distributed/cluster；Neo4j 的集群或受控恢复策略须按所选版本与许可落地 | 节点故障、索引重建、备份恢复后无跨租户召回或图谱关联 |
| 缓存与队列 | Redis HA；Celery worker 多副本、任务幂等、死信和补偿任务 | worker 被终止或重复投递时，`operation_id` 不产生第二次业务副作用 |
| 模型与观测 | 内网模型端点池、Langfuse/Prometheus/Grafana 自部署、告警和审计 | 模型不可用时，敏感写操作转人工；监控告警与审计链路仍可追溯 |
| 备份与灾备 | 加密备份、恢复剧本、权限最小化；上线前确认 RPO/RTO | 首期目标暂定 RPO 不高于 15 分钟、RTO 不高于 60 分钟，必须以租赁服务器演练记录确认，不可仅凭设计宣称达标 |

---

## 5. 组件选型一览表（速查）

| 层 | 组件 | 选型建议 |
|---|---|---|
| 前端框架 | React + Next.js | ✅ |
| UI 组件库 | Ant Design | ✅ |
| AI 交互 | 自定义 SSE transport/parser；Vercel AI SDK 仅可经协议适配器使用 | ✅（设计态） |
| 后端 API | FastAPI（REST + SSE） | ✅ |
| 编排 | LangGraph + CrewAI | ✅（设计态） |
| 业务数据 | PostgreSQL | ✅ |
| 记忆图谱 | Neo4j（Graphiti） | ✅（见记忆架构设计） |
| 向量检索 | 开发/预发布 Milvus standalone；生产 Milvus distributed/cluster | 生产拓扑待部署验收 |
| 可观测性 | 自托管 Langfuse（+ Prometheus/Grafana） | ✅ |
| 部署 | 自建 Nginx / Docker Compose / K8s | 开发、预发布、生产分层；租赁 IaaS 可承载自托管环境 |

---

## 6. 与现有方案的关系

- **保留**：LangGraph 主控、CrewAI 子智能体、Agentic RAG、唯一人工审批、记忆层（Graphiti + Neo4j）。
- **升级**：UI 从 Streamlit → React + Next.js；可观测云服务 → **自托管 Langfuse**；数据层采用 Neo4j + Milvus，并明确 standalone 仅用于验证、生产使用 HA 拓扑。
- **Streamlit/Chainlit**：降级为**内部演示/原型**，不再作为生产界面。

---

## 7. 答辩叙事（可直接背）

> "生产目标采用前后端分离：React + Next.js 前端、FastAPI 的 REST + SSE 网关、LangGraph + CrewAI + MCP 核心和人工审批。开发时用固定版本 Compose 验证，线上预发布可以部署到我方租赁服务器；真正生产采用多副本应用、PostgreSQL 主备、Milvus 分布式拓扑、Redis HA、备份恢复和监控告警。Graphiti + Neo4j 负责长期记忆。全程由项目方自运维，不依赖公有 SaaS；租赁服务器只是承载资源，不等于把关键服务外包。"

**高频追问应答**：
- *"为什么不用 Chainlit？"* → 它面向对话原型，性能/扩展、品牌定制、企业集成（权限/审计/看板）都有限；生产用专业前端框架 + 前后端分离更可持续。
- *"前后端怎么通信？"* → `POST /api/chat` 做唯一 AI SSE 流，`/api` REST 管会话、审批和操作查询；前端以自定义 parser 或协议适配器消费六种冻结事件。
- *"怎么扩展？"* → 前端、API 和 worker 水平扩容；Milvus 用生产分布式拓扑，数据库按主备/恢复方案建设，并以故障和恢复演练作为上线门槛。
- *"能上云吗？"* → 可以租赁 IaaS 服务器做预发布或生产承载，但 LLM、数据库、向量库、图谱和可观测仍由项目方自部署、自管密钥和备份；接入公有 SaaS LLM 或云端可观测则必须单独披露数据流与合规影响。

---

*文档版本 1.2 · 2026-09-03 · 生产环境架构设计 · 与「项目架构说明书 / 记忆架构设计 / 工具调用与集成 / 生产基线与验收测试」配套。*
