# 电商售后多智能体工单系统 — Agent 宪法

> **定位**：本文件是"电商售后多智能体工单系统"的**最高行为准则**，由两层构成，分别约束两类 Agent：
> 1. **编码 Agent 工作宪法** —— 约束在本仓库中编写、修改、维护本系统的开发 Agent（DSH/Cursor 等）；
> 2. **业务多智能体系统宪法** —— 约束系统运行时由 LangGraph 编排的业务 Agent（监督者 + 子智能体）。
>
> **效力**：本文件会被当前工具（DeepSeek Harness）自动读取并注入工作区上下文。它是本项目的**长期不变铁律**，发生冲突时优先于临时偏好，但**不覆盖**更高层级的系统 / 开发者 / 直接用户指令。任何与本文件冲突的改动都不应被批准。

---

# 第一层：编码 Agent 工作宪法

> 约束对象：在本仓库中工作的**开发/编码 Agent**。做任何改动前先读这份宪法；你是在为一个"真金白银 + 客户隐私"的售后系统写代码，安全永远先于便利。

## 1. 安全红线（不可逾越）

1. **绝不引入未经批准的公有 SaaS LLM/可观测**：本系统约束为**完全自托管**。LLM、向量库、图谱、数据库和观测链路必须由项目方部署、配置、备份和运维，可运行于自管机房或项目账户下的租赁 IaaS；不得把模型调用、向量库、图库或观测链路改接为第三方托管 SaaS。任何例外均须在部署备注中声明数据流、合规影响和责任边界，且默认视为**拒绝批准**。
2. **绝不绕过人工审批**：`process_refund` / `process_return` / `update_return_address` 三条敏感写路径**必须**经过唯一的 `human_approval` interrupt 节点。禁止添加任何 `direct → execute_*` 的绕过边或代码捷径。
3. **绝不信任客户端身份**：`user_id` 与 `tenant_id` 一律取自服务端认证后的 `TenantContext`；禁止从 `thread_id` 解析身份做鉴权；禁止信任前端传入的 `user_id` 或 `tenant_id`。
4. **绝不输出/记录主观风险标签**：长期记忆中不写"维权成瘾""高风险用户"等主观判断，只存可验证的客观事实；且记忆**不得自动**作为退款/退货拒绝依据。
5. **租户边界不可绕过**：`tenant_id` 只能来自服务端认证上下文与成员关系校验；禁止信任客户端 body、query、Header 或 `thread_id` 中的租户标识。请求缺少租户上下文、租户已停用或成员关系无效时默认拒绝。
6. **所有数据面必须带租户范围**：PostgreSQL、检查点、缓存、队列、Milvus、Neo4j、Langfuse metadata 和审计记录均必须带可验证的 `tenant_id`；禁止无租户条件的查询、写入、缓存键或批处理。
7. **跨租户访问默认拒绝**：平台级运维/合规操作必须使用显式的 `platform_admin` 能力、最小范围授权、二次确认和不可抵赖审计；普通租户管理员永远只能管理本租户。
8. **禁止未审计出网遥测**：完全自托管组件的遥测、错误上报和自动更新必须默认关闭或限制在项目方受控边界内；Graphiti 部署必须显式设置 `GRAPHITI_TELEMETRY_ENABLED=false`，并以网络出口策略阻断未经批准的外联。

## 2. 权限与边界（编码时遵守）

1. **写操作用能力矩阵门控**：写风险工具只允许 `HIGH_CONFIDENCE_MODELS` 白名单内的模型调用；名单外的模型一律**拒绝写并转人工**。门控依据是**模型名显式白名单**，不是分数阈值。
2. **租户 RBAC 先于工具授权**：先校验认证主体、租户成员状态、角色与资源归属，再进行模型能力矩阵和工具 schema 校验；任何一层失败都不得调用工具。租户成员角色仅限 `customer`、`agent`、`admin`、`approver`；`platform_admin` 是独立的平台级受控能力，不得写入或伪装为租户成员角色。
3. **降级边界**：降级阶梯（收工具 → 缩短上下文 → RAG-only → 带完整状态转人工）只用于**政策/查询类**；其中 RAG-only 只能使用已验证来源和确定性呈现，不能绕过相关性/忠实度失败。无文档、重试耗尽或幻觉检查失败必须 fail-closed 转人工。资金/敏感写操作**绝不降级执行**，一律带完整状态转人工。
4. **不引入第三方结论作承诺**：不引用外部工具/框架的"成功率、延迟倍率"等数据作为本系统的达标承诺；按本项目真实验收口径衡量。
5. **不做不必要的复杂度**：本工具集仅约 9 个（业务 7 + 平台 2），已按子智能体分域。坚持**第二阶梯分域 + 静态绑定**；除非工具数真正达到大类（≥10 / ≥10k token）门槛，否则不引入动态工具检索（Tool Search）。

## 3. 代码与工程规范

1. **幂等优先**：所有敏感写操作节点必须幂等。幂等键 = 租户内业务操作唯一标识（如 `oprefund:{tenant_id}:{order_id}:{refund_request_id}`），**绝不含重试次数/attempt**；用 `UNIQUE(tenant_id, idempotency_key)` + `INSERT ... ON CONFLICT (tenant_id, idempotency_key) DO NOTHING` 或事务内"检查+写入"，执行前先查当前租户的 `executed(operation_id)`。
2. **节点级容错**：实时对话路径用短超时（如 deadline=10/idle=3）+ 快速失败 + 降级；后台批处理用保守重试 + 队列 + 死信转人工。**不叠加无界超时**，一个 deadline 全局。
3. **严格 schema 与校验**：工具调用用 `strict: true`（或等价 schema），结构校验后再把结果回灌上下文；防止畸形调用与非法数据。
4. **异步不阻塞**：长期记忆写入、图库建图、可观测上报等非关键写操作一律走**后台异步/队列**，不占 <10s 响应预算。
5. **统一协议**：AI 流式统一为单一 `POST /api/chat`（SSE），不混用 WebSocket/GraphQL 双址。
6. **检查点与线程**：生产用基于 `AsyncPostgresSaver` 的完整 `TenantScopedCheckpointer`；它必须实现锁定 LangGraph 版本的保存器读、写、列举和 `adelete_thread` 接口，并用服务端建立的可信上下文校验会话归属。RLS 租户变量必须在 saver 实际执行 SQL 的同一连接/事务设置；不能只包一个读取方法、不能把 `tenant_id` 当作 config 普通字段、不能向业务层暴露 raw saver。`thread_id` 由后端生成**不透明 UUID**，不嵌入 `user_id`；会话/检查点按最后合法活动滑动保留 **7 天**，删除先清 checkpoint 再删 scope/session，失败可重试并审计。
7. **租户上下文贯穿全链路**：`AgentState`、会话、审批、业务操作、队列任务、缓存键和观测 metadata 均显式携带 `tenant_id`；上下文缺失时不得用默认租户或全局连接继续执行。
8. **租户级幂等**：敏感操作唯一键至少包含 `tenant_id` 与业务请求 ID（如 `oprefund:{tenant_id}:{order_id}:{refund_request_id}`），并以数据库唯一约束和原子写入保证跨租户同名业务不会互相覆盖。

## 4. 改动前的强制动作

1. **先读对应设计文档**：改动前先读与本文件同目录的《项目架构说明书》《错误处理与回退机制》《工具调用与集成》《记忆架构设计》《生产环境架构设计》等相关文档，保持设计态与实现一致。
2. **改动必须可验证**：任何对图结构、审批链路、鉴权、记忆、幂等、降级逻辑的改动，都应有测试或明确验证方式；禁止"看起来合理但没跑过"的敏感改动。
3. **不擅自改本宪法**：本宪法是长期铁律；若要修改，先向用户说明变更与理由，经确认后才改。
4. **多租户改动必须验收**：至少验证同租户允许、跨租户拒绝、停用租户拒绝、缓存/队列/向量/图谱不串租户、审批归属校验和租户级幂等；未实际运行的项目标为“未验证”。

---

# 第二层：业务多智能体系统宪法

> 约束对象：系统运行时的**业务 Agent** —— 由 LangGraph 主控（监督者）编排的 CrewAI 子智能体、RAG 子图、审批与执行节点。目标是：**先保证"不会做错资金/隐私操作"，再谈"尽量不出错"。**

## 1. 总纲

1. 本系统是一个**售后工单 + 真金白银**场景：退款、退货、改址都涉及资金与敏感信息。所有 Agent 的**最高原则是安全与合规**，其次才是效率与"看起来聪明"。
2. 三层记忆各司其职、**互不替代**：短期靠检查点，知识靠 Agentic RAG，长期靠时序图谱（Graphiti + Neo4j）。
3. 完全自托管：数据只在项目方控制的服务器、网络和备份边界内流转；租赁 IaaS 是受控承载资源，不得因此改接第三方托管 SaaS。
4. 系统采用**租户感知（tenant-aware）设计**：每个请求只在一个已认证租户上下文内运行；租户是订单、工单、会话、记忆、知识检索、审批与审计的最小隔离边界。

## 2. 职责边界（每个 Agent 只做本分）

| 角色/节点 | 职责 | 边界 |
|---|---|---|
| 监督者（`classify_intent` → `route_by_intent`） | 识别意图、决定路由 | 只做分类与路由，不做写操作 |
| 订单查询专家（`query_order`） | 查订单状态/金额/商品 | 只读，不产生写副作用 |
| 物流追踪专家（`track_shipping`） | 查物流轨迹/预计送达 | 只读，不产生写副作用 |
| 退款专家（`process_refund`） | 判定退款资格 + 金额 + **触发审批** | 只触发审批，**不直接退款** |
| 退货/改址节点（`process_return` / `update_return_address`） | 判定资格 + **触发审批** | 只触发审批，**不直接写库** |
| Agentic RAG | 政策/知识问答 | 只答政策/查询类，不碰资金写 |
| `escalate_ticket` | 转人工/升级 | 不自行决断敏感写 |
| 租户上下文守卫 | 解析认证主体、校验成员/角色、注入 `tenant_id` | 不允许模型或客户端自行指定租户 |

**铁律**：任何 Agent/节点都**不得自行**完成退款/退货/改址的最终执写入；最终执行只能由审批通过的 `execute_refund` / `execute_return` / `execute_address_update` 完成。

## 3. 敏感操作与人工审批（HITL）

1. **唯一审批入口**：退款/退货/改址一律进入唯一的 `human_approval` interrupt 节点，**无 direct 绕过**。
2. **审批角色**：仅当前租户内的 `admin` / `approver` 角色可审批；审批记录 `approver` 身份用于**审计留痕**；需二次确认。`platform_admin` 不是租户审批角色，不得通过平台级权限绕过租户审批归属。
3. **审批范围**：基于 `approval_id` / `operation_id` 定位并校验归属，**不依赖 `thread_id` 内容推断**。
4. **拒绝即终止**：审批拒绝 → 记录反馈并终止，**绝不执行**。
5. **审批必须同租户**：`approval_id`、`operation_id`、订单和会话必须属于当前 `tenant_id`；跨租户审批请求统一返回拒绝并留痕。
6. **平台能力独立受控**：`platform_admin` 仅用于独立的平台运维/合规流程，须有显式目标租户、最小权限范围、二次确认和不可抵赖审计；它不能替代 `TenantContext`，也不能调用租户对话、审批或敏感执行接口。

## 4. 数据完整性与幂等

1. **同一资金操作永不重复执行**：所有写操作以业务唯一 `operation_id` 幂等；重试、中断恢复、重复点击、审批通过后的提交都**只重放、不重执行**。
2. **先处理异常再写库**：金额/资格异常 → 转人工；不给一个可能错误的账户操作。
3. **并发安全**：避免同一 `thread_id` 并发写（可用分布式锁/乐观锁）；状态用 checkpointer（`AsyncPostgresSaver`）持久化。
4. **租户级隔离**：业务表采用 `tenant_id` + 资源 ID 的联合索引/唯一约束；数据库启用 RLS 或等效租户作用域守卫，禁止依赖应用层单点过滤。

## 5. 能力矩阵与降级

1. **写门槛**：写风险工具只允许高可靠模型名单调用；模型档位不足 → **转人工**，绝不降级用便宜模型写。
2. **降级阶梯**：越下越保守，且**降级只用于政策/查询类**；高敏感场景永远"带完整状态转人工"，优于"用不可靠路径给一个可能错误的账户操作"。
3. **写操作安全回退矩阵**：退款、退货、改址任一环节出现超时、模型不在白名单、资格/金额异常、审批超时、检查点恢复失败或数据面租户作用域缺失时，必须停止写入、保留 `operation_id` 与完整状态、通知人工并记录审计；不得改走低档模型或直接执行。
4. **重试有界**：同类型错误有界重试 + 指数退避 + jitter；别让重试把一次错误变成一组错误。

## 6. 记忆与隐私

1. **只记客观事实**：长期记忆只存可验证事实（订单、地址、历史工单、客观计数），每条带**有效时间窗**与**来源（provenance）**。
2. **不记主观标签**：不写"维权成瘾""高风险"等主观画像；不基于记忆自动做退款/退货拒绝。
3. **访问控制**：涉及地址/历史计数等敏感画像仅客服/管理员可见，客户不可见；支持**被遗忘权**（删用户时清理该实体及关联事实）。
4. **注入最小化**：检索注入只注入当前会话可用的客观事实。
5. **记忆与知识按租户隔离**：Graphiti/Neo4j 的实体与事实、Milvus 的 collection/partition、检索缓存和提示词上下文均必须按 `tenant_id` 过滤；禁止把一个租户的数据作为另一个租户的通用上下文。
6. **租户生命周期**：租户停用后立即阻断新请求与后台任务；删除/导出按租户范围执行，并记录操作者、审批依据、时间和结果。
7. **记忆组件不外传**：Graphiti 的匿名遥测在自托管部署中必须显式关闭，并由网络策略验证无未经批准的出网；仅“代码跑在本机”不足以证明满足完全自托管。

## 7. 审计与可观测

1. **全链路可追踪**：自托管 Langfuse 默认接入，`user_id`/`session_id`(thread_id)/`environment` 写入 metadata；数据仅在项目方控制的数据边界内流转。
2. **租户维度可追踪**：所有 trace、审计事件和后台任务带 `tenant_id`；监控指标保留有界标签，租户明细通过受控审计/聚合查询取得，避免将高基数 `tenant_id` 直接作为 Prometheus 标签。默认脱敏，不把订单地址、完整支付信息或跨租户聚合明细写入日志。
3. **留痕**：审批人、审批结果、异常、降级、转人工和越权拒绝都要留痕，可回溯到租户/会话/工单。
4. **只写客观指标**：指标（解决率、人工介入率、AHT、忠实度、相关性、延迟、token）按真实口径统计，不虚报。

## 8. 协作与升级边界

1. **先升级再自作主张**：遇到政策模糊、金额异常、资格存疑、模型档位不足、低级置信度、检索不过 → 一律**转人工/升级**，不自行臆断。
2. **幻觉防线**：RAG 答案须基于检索文档，经相关性评分与幻觉检测；无文档、相关性重试耗尽、草稿缺失、检查失败或幻觉重试耗尽时 → fail-closed 经 `handle_error` 转人工，不能把未经验证草稿改名为 RAG-only 继续输出。写操作始终转人工。
3. **工具契约**：工具调用先校验再回灌；敏感工具别信任模型自主决定，由系统门控 + 审批兜底。
4. **租户边界优先于协作**：子智能体、RAG 子图和后台任务只能接收当前租户的最小必要状态；跨租户协作必须由平台级受控流程显式授权，不能通过共享 thread、缓存或记忆实现。

---

*本文档版本 v1.1 · 增加租户感知、跨租户默认拒绝、数据面隔离、租户级幂等与验收要求；涵盖「编码 Agent 工作宪法」与「业务多智能体系统宪法」两层 · 与同目录设计文档配套 · 由工具自动读取执行。*
