# 电商售后多智能体工单系统 — 生产环境基线（Production Baseline）与验收测试

> **定位**：本文件**冻结"原型交付范围"与"生产目标架构"的边界**，列明**已知边界 / 后续迭代项**，把**错误回退从"概念"落到"可执行接口/结构"**，并提供**生产验收测试清单**。
> **结论**：项目定位为**生产级设计目标**，不是已经运行的生产系统。文档将本地开发、租赁服务器上的线上预发布验证、正式生产三层明确区分；多租户隔离从 API、存储到 Agent 数据面都是不可降级的基础约束。生产级结论只能由后续部署、压测、故障和恢复演练的记录证明。

---

## 一、定位与范围分层

**核心原则**：不以缩减生产目标来回避复杂度，而是将实现和证据分层。单机 Compose 只能证明功能闭环；租赁服务器上的预发布环境验证真实网络与运维；只有生产拓扑和验收门槛同时完成，才允许对外作生产就绪声明。

| 维度 | **本地开发/功能验证** | **线上预发布验证（规划）** | **生产目标架构** |
|---|---|---|---|
| 部署 | 单机 Docker Compose；Milvus standalone | 项目账户下的租赁 IaaS VM 或小型 K8s，部署真实镜像、TLS、密钥和监控 | K8s/等效编排、Ingress/LB、多副本、私有数据面、弹性扩容 |
| 数据 | 模拟数据（mock_db / mock 电商 API） | 脱敏或合成数据；验证备份、恢复、网络与权限 | 真实电商/支付/物流 API，按合规和数据驻留要求接入 |
| 用户与租户 | 多租户基础隔离（认证主体 + 成员关系 + 会话/资源归属；最小 RBAC） | 两个及以上租户的端到端隔离、停用和越权审计验证 | 完整 RBAC、租户生命周期治理、配额与资源策略 |
| 支付/退款 | 审批后**写库标记 + 可替换提交接口** | 验证审批、幂等、补偿和人工交接，不接真实资金 | 真实支付通道 + **对账补偿** |
| 审批 | 退款/退货/改址**一律人工审批** | 模型/工具/审批失败均安全转人工 | 保留审批 + 风控规则辅助 |
| 合规 | 最小 PII 保护（脱敏、日志保留、删除） | 明确服务器地域、访问边界、备份位置和供应商责任 | 数据地域、审计、加密、导出；自托管运维 |
| 高可用 | 单实例（检查点可恢复） | 验证单节点故障、重启、备份恢复和告警，不宣称 HA | 高可用、RPO/RTO、容量压测和灾备演练 |

> **当前证据边界**：本目录为纯设计文档，尚无可运行服务、迁移脚本或线上部署记录。因此表内“开发/预发布/生产”均为设计与验收计划，绝不表示已经完成验证。

---

## 二、已知边界与后续迭代项（作为未来项列明，非本次实现）

| 边界/未来项 | 现状（原型） | 生产目标（后续迭代） |
|---|---|---|
| RBAC 完整权限 | 基础多租户成员校验 + 会话/资源归属 + 审批人角色 | 客户/客服/管理员/审批人/平台运维多角色 + 细粒度授权 |
| 租户治理 | `tenant_id` 作用域、跨租户默认拒绝、共享库 RLS/等效 guard | 租户开通/停用/删除/导出、配额、独立 schema/数据库选项 |
| 支付对账 | 审批后写库标记 + 提交接口 | 真实支付通道 + 对账补偿任务 |
| PII 合规 | 脱敏、日志保留、删除接口 | 加密、数据地域、导出、审计合规 |
| Prompt Injection 防护 | 系统提示 + 工具参数校验 | 输入净化 + 知识文档白名单 + 越权检测 |
| 知识库治理 | 单版本政策 | 版本/生效日期/冲突策略/过期处理 |
| CI/CD / 运维 | 手动 | 锁版本、迁移脚本、密钥轮换、漏洞扫描、发布回滚 |
| 备份恢复 | 检查点保留 7 天；Compose 卷仅用于开发验证 | 备份加密、恢复剧本、RPO/RTO、健康检查、容量上限、压测目标 |
| 可观测 | 自托管 Langfuse | 自托管 Langfuse + Prometheus/Grafana |
| 线上预发布 | 无 | 租赁服务器网络分区、TLS、密钥管理、告警、备份和回滚演练 |

> 说明：上述均为**生产目标迭代项**，原型阶段不实现；但对外 API 设计契约已在 §三.1 冻结，实际接口、OpenAPI 和验收证据仍待后续实现。

---

## 三、错误回退的可执行基础设施（从"概念"→"接口/结构"）

审计指出：队列、死信、熔断、补偿、人工升级只有概念、无数据结构/服务/依赖/API。此处给出**可执行的组件、依赖与接口定义**（原型用轻量实现，生产可替换）：

| 组件 | 依赖 | 用途 / 接口 |
|---|---|---|
| **任务队列** | Redis（Broker）+ **Celery** | 异步记忆写入、批处理重试；`queue.enqueue(task)` |
| **死信表（DLQ）** | PostgreSQL | 重试超限的任务落库；`INSERT dlq(task_id, payload, error, ts)` |
| **熔断器** | 进程内/Redis 共享 | `CircuitBreaker.open()/half_open()/closed()`；失败阈值+冷却+半开探测 |
| **补偿/对账任务** | PostgreSQL | 审批后写库失败/超时的补偿；`reconcile(op_id)` |
| **人工升级服务** | FastAPI | 后端领域服务 `create_escalation(...)` 生成工单、通知审批人、带完整状态移交；不暴露第二个 `/escalate` 公共入口 |
| **LLM 回退链** | 本地/内网 OpenAI 兼容端点 | 错误分类 + 有界重试 + 熔断 + 降级（见《错误处理与回退机制》） |

**依赖清单（本地 Docker Compose 验证组件）**：PostgreSQL、固定版本 **Milvus standalone**（含 etcd + MinIO）、Neo4j、Redis（Broker + 锁）、Celery worker、MCP 服务、FastAPI 网关（详见《项目落地细节》§5）。此清单不构成生产 HA 拓扑；生产由《生产环境架构设计》§4.2 的多副本和数据服务韧性要求约束。

> **队列口径**：全包统一使用 **Celery（Python）+ Redis Broker**。**不采用 BullMQ/Node 方案**，以保持全 Python 技术栈、与 LangGraph/FastAPI/CrewAI 同生态，避免混用两套语言与依赖。

### 3.1 冻结的对外 API 与恢复契约（设计态）

AI 流唯一入口是 `POST /api/chat`，响应必须为 `text/event-stream`；没有 JSON `ChatResponse`、WebSocket 或第二条聊天入口。该契约已经作为设计基线冻结，但仓库尚无可运行服务，下面所有行为均须由后续 OpenAPI、集成和预发布测试验证。

| 调用 | 规定 |
|---|---|
| 新流 | body 为 `{mode:"start", thread_id, client_request_id, message}`；三项业务字段均必填，`thread_id` 为服务端创建会话时签发的 ID。 |
| 续传 | body 为 `{mode:"resume", stream_id}`，并强制请求头 `Last-Event-ID`。服务端只重放游标之后的已持久化事件并附着既有流，不可由此重新执行图或工具。 |
| 身份与去重 | 服务端从认证上下文确定用户和租户；以 `(tenant_id, user_id, client_request_id)` 对 `start` 原子去重。客户端不得传 `tenant_id`。 |
| 帧结构 | 每个 SSE 帧都有严格递增的 `id`、六选一的 `event` 和 JSON `data`。`event` 只允许 `accepted`、`token`、`node`、`approval_required`、`done`、`error`。 |
| 事件语义 | `accepted` 返回 `stream_id`；`approval_required` 返回 `approval_id` 与 `operation_id`；`done` 结束流；`error` 给出受控失败信息。前端按 `(stream_id, id)` 去重。 |
| 敏感写 | 断线、刷新和取消不得自动重放聊天或审批写请求。审批结果未知时先查 `GET /api/approvals/{approval_id}`，再查 `GET /api/operations/{operation_id}`，以服务端状态为准。 |

REST 资源统一使用 `/api` 前缀：`POST/GET /api/sessions`、`GET /api/sessions/{thread_id}`、`GET /api/sessions/{thread_id}/messages`、`GET /api/approvals`、`GET /api/approvals/{approval_id}`、`POST /api/approvals/{approval_id}/decision`、`GET /api/operations/{operation_id}`。所有入口都必须重新校验当前 `TenantContext`；无权访问时不泄露其他租户资源是否存在。

---

## 四、多租户迁移闸门

当前仓库未包含可迁移的业务应用源码、业务 PostgreSQL schema 或 SQLite 数据文件；因此本次只冻结迁移契约，**没有执行任何猜测性数据迁移**。实际接入业务库时必须按以下顺序执行：

1. **盘点与映射**：列出每张业务表、检查点、审批、操作、审计、缓存、队列、Milvus 与 Neo4j 数据的归属来源，形成经过业务确认的“旧记录 → tenant_id”映射。
2. **先建租户主数据**：创建 `tenants`、`tenant_memberships` 与平台级授权记录；停用、删除中、成员撤销的租户不能建立新会话或消费新任务。
3. **可逆回填**：先添加可空 `tenant_id`，按已确认映射分批回填并记录批次/校验结果；**禁止把无法判定归属的记录默认为某个租户**，应隔离并转人工核对。
4. **约束与 RLS**：回填核对为零遗漏后，改为 `NOT NULL`，补 `FOREIGN KEY`、`UNIQUE(tenant_id, resource_id)` 与 RLS/等效 guard；查询仓库只能暴露带租户参数的方法。
5. **数据面重建**：为 Milvus 重建租户 collection/partition/filter 索引；为 Neo4j 写入 `tenant_id` 并建立复合约束；将 Redis key、Celery payload、Langfuse trace/audit 补齐租户上下文。
6. **灰度切换与回滚**：先仅允许读取已迁移租户，完成隔离测试后再开放写入；保留可追溯迁移批次和只读回滚路径。出现归属不确定或串租户证据时，立即停止切换并阻断相关租户写操作。

最小 RLS 验收模式：在**实际执行 SQL 的同一连接和事务**中，由受控保存器或 repository 以参数化方式设置已验证的 `app.tenant_id`；应用代码不得拼接该值或从客户端转发。`TenantScopedCheckpointer` 的 `ContextVar` 是授权来源，`config.configurable.tenant_id` 只能用于可观测性，不能替代该来源。

```sql
-- DDL/迁移角色与运行角色分离。app_runtime 不得为超级用户、BYPASSRLS 或这些表的 owner。
CREATE ROLE app_runtime LOGIN NOINHERIT NOSUPERUSER NOBYPASSRLS;

CREATE FUNCTION app_current_tenant_id() RETURNS TEXT
LANGUAGE sql STABLE
AS $$ SELECT NULLIF(current_setting('app.tenant_id', true), '') $$;

ALTER TABLE sessions ENABLE ROW LEVEL SECURITY;
ALTER TABLE sessions FORCE ROW LEVEL SECURITY;
ALTER TABLE checkpoint_thread_scopes ENABLE ROW LEVEL SECURITY;
ALTER TABLE checkpoint_thread_scopes FORCE ROW LEVEL SECURITY;

CREATE POLICY sessions_tenant_scope ON sessions
    USING (tenant_id = app_current_tenant_id())
    WITH CHECK (tenant_id = app_current_tenant_id());

CREATE POLICY checkpoint_scope_tenant_scope ON checkpoint_thread_scopes
    USING (tenant_id = app_current_tenant_id())
    WITH CHECK (tenant_id = app_current_tenant_id());

-- 供已核验的官方 checkpoint 表 policy 使用；正常请求是否允许 active/deleting
-- 另由 TenantScopedCheckpointer 的可信 scope 校验决定，不能仅依赖这个函数。
CREATE FUNCTION checkpoint_thread_in_current_tenant(candidate_thread_id TEXT) RETURNS BOOLEAN
LANGUAGE sql STABLE
AS $$
    SELECT EXISTS (
        SELECT 1
        FROM checkpoint_thread_scopes AS s
        WHERE s.thread_id = candidate_thread_id
          AND s.tenant_id = app_current_tenant_id()
    )
$$;
```

`langgraph-checkpoint-postgres==3.1.2` 首次建表后，迁移必须先记录实际表名、thread 标识列、blob/write 关联方式和 owner，再对**每张**官方 checkpoint、blob、write 表执行 `ENABLE ROW LEVEL SECURITY` 与 `FORCE ROW LEVEL SECURITY`。若表行带 `thread_id`，policy 必须按 `checkpoint_thread_in_current_tenant(thread_id)` 限制；若关联方式不同，则以同一语义改写为存在性关联。不得因为官方 schema 未核验就跳过 RLS、只保护映射表，或将 raw saver 暴露给业务代码。运行角色的最小 `GRANT`、事务内 `set_config('app.tenant_id', verified_tenant, true)` 和表级 policy 必须随安装验收记录一并保存。

---

## 五、线上预发布与生产验收测试清单

先在租赁服务器的预发布环境完成网络、隔离、故障和恢复验证，再进行正式上线验收。每个用例都应沉淀为带环境版本、时间、监控截图/日志位置和结论的记录；本文档没有任何一项已执行记录。

| 类别 | 用例 | 预期 |
|---|---|---|
| **异常** | 工具查询超时 | 重试→换工具→告知稍后→转人工，不挂死 |
| 异常 | LLM 调用 429/5xx（政策/查询） | 有界重试→受限降级→转人工，不裸等 |
| 异常 | LLM 调用 429/5xx（退款/退货/改址） | 不切换到低能力模型执行写路径；保存完整状态、通知审批/人工并安全交接 |
| 异常 | RAG 检索为空 | 不 `max([])` 报错，转拒答/转人工 |
| 异常 | 幻觉检测超上限 | 重试 N 次后拒绝 + 转人工，不硬答 |
| **API/SSE 契约** | `start` 请求、首帧与事件字典 | 只接受规定的 `mode=start` 字段；首帧为 `accepted`；每帧均有 `id/event/data(JSON)`，事件只来自六种冻结值。 |
| API/SSE 契约 | 断线后的 `resume` | 必须含 `stream_id` 和 `Last-Event-ID`；只补发后续事件且前端去重，不重新启动图、工具或敏感写。 |
| API/SSE 契约 | 自定义前端 parser/适配器 | 默认 Vercel AI SDK `useChat` 不得被当作可直接解析该自定义 SSE 的证据；必须有 parser 或协议适配器的集成测试。 |
| **并发** | 同一 thread 并发写 | 分布式锁/乐观锁，状态不冲突 |
| 并发 | 多用户同租户并发 | 会话隔离，互不越权访问 |
| **租户隔离** | 同一用户切换租户 | 仅能看到当前认证租户的数据；客户端不能覆盖 `tenant_id` |
| 租户隔离 | 租户 A 读取/审批/操作租户 B 的资源 | 404/403 拒绝、无状态泄露、记录越权审计；不得恢复图执行 |
| 租户隔离 | 两租户存在同名订单/请求 ID | 联合唯一键隔离；不串订单、不串审批、不串幂等结果 |
| 租户隔离 | RAG/Neo4j/Redis/Celery 数据面探测 | 召回、图查询、缓存和任务消费均无跨租户数据 |
| 租户生命周期 | 租户停用或成员撤销 | 新请求、重连恢复与后台任务均被阻断；敏感在途操作转人工 |
| 检查点作用域 | 同一 `thread_id` 被另一租户恢复、查询历史或审批续跑 | `TenantScopedCheckpointer` 在每个入口拒绝，图执行器不启动并写越权审计 |
| 检查点 RLS | 租户 A 的运行角色直接查询租户 B 的 scope 及已安装官方 checkpoint/blob/write 表 | 设置 A 的 `app.tenant_id` 后零可见、零可写；未设置该变量同样拒绝。`FORCE RLS` 对表 owner 的绕过也必须被验证。 |
| 检查点连接闭环 | 图读写、历史恢复、审批续跑及删除 | 通过连接/事务级探针证明 `set_config(..., true)` 与 saver SQL 使用同一连接；禁止先在业务连接设置、再由 raw saver 换连接执行。 |
| 检查点适配器 | 编译图、恢复、历史、写入、删除 | 仅 `TenantScopedCheckpointer` 可传给 `graph.compile`；覆盖锁定版本实际调用的完整保存器接口，raw `AsyncPostgresSaver` 无业务可达路径。 |
| 检查点清理 | scope 到期、租户删除、`adelete_thread` 失败后重试 | 先 claim 并标记 `deleting`，成功删除 checkpoint/blob/write 后才删除 scope/session；失败保留定位信息、递增尝试、告警并可幂等重试。 |
| **重复审批** | 同一审批重复提交 | 幂等键挡住，退回同一结果，不重复执行 |
| 重复审批 | 决策请求断线或响应丢失 | 不自动重发写请求；由审批详情取得 `operation_id` 后查询操作状态，零重复业务副作用。 |
| 重复审批 | 审批人冲突/超时 | 超时自动升级；冲突提示 + 二次确认 |
| 重复审批 | 驳回后重新发起 | 生成新业务请求，原 op 不受影响 |
| 重复审批 | 高金额 | 触发**双人复核**或升级（不单人放行） |
| **幂等** | 同一租户同一 op 重放 | `UNIQUE(tenant_id, idempotency_key)` + 原子写，只执行一次 |
| 超时写入 | 审批通过后写库失败 | 补偿任务重试，不丢业务副作用 |
| **数据删除** | 用户删除 | 仅在指定租户内清理记忆图谱 + 会话 + 工单（"被遗忘权"） |
| 数据删除 | 租户删除 | 先停用，再按可审计任务清理其 PostgreSQL/Neo4j/Milvus/缓存/队列数据 |
| 数据删除 | 审计/日志保留 | 按保留策略，敏感信息脱敏 |
| **线上故障** | 关闭一个 API 副本 / 一个 Celery worker | 服务继续处理可恢复读请求；在途敏感操作不重复执行，必要时转人工 |
| 线上故障 | 重启 Redis、PostgreSQL 备节点或数据服务节点 | 依据所选生产拓扑恢复；任务、会话、审批和租户边界不丢失、不串租户 |
| 备份恢复 | 从 PostgreSQL、Neo4j、Milvus 的独立备份恢复到隔离环境 | 数据完整性、租户隔离、索引/图谱查询和审计链路通过复验 |
| 灾备目标 | 计时恢复演练 | 首期暂定 RPO ≤ 15 分钟、RTO ≤ 60 分钟；未达到则不能宣称满足该目标 |
| 容量与告警 | 经批准的并发/混合业务压测与告警触发 | 达到业务 SLO；零跨租户数据泄露、零重复敏感写；阈值、成本和扩容策略形成记录 |

---

## 六、P0 硬伤修复分布（各文档）

| 硬伤 | 修复位置 |
|---|---|
| P0-1 定位分层 | 本文件 §一；需求说明书"不包含范围"改为"已知边界" |
| P0-2 主图闭环 | 项目说明书 graph.py；工具调用与集成 |
| P0-3 审批后执行 | 项目说明书 / 架构说明书（补执行节点） |
| P0-4 认证权限 | 项目说明书 API；会话与线程管理（审批人校验 + 服务端鉴权） |
| P0-5 幂等失效 | 错误处理与回退机制（op_id + 唯一约束） |
| P0-6 回退落地 | 本文件 §三 |
| P0-7 意图与审批切分 | 项目说明书 / 架构说明书（return / return_address 独立意图，一律人工） |
| P0-8 队列/依赖一致性 | 项目落地细节 §5（弃 BullMQ，统一 Celery；补依赖） |
| P0-9 多租户隔离 | Agent 宪法、会话/线程、项目说明书、记忆与生产架构；租户作用域贯穿 API/DB/RAG/图谱/缓存/队列/审计 |
| P0-10 单一 AI 流与恢复 | 生产环境架构 §3、前端体验 §5、本文件 §3.1；`POST /api/chat` + 六种 SSE 事件 + `Last-Event-ID` + `operation_id` 查询 |

---

*版本 1.4 · 2026-09-03 · 多租户生产基线与验收测试 · 与「需求/架构/落地/记忆/工具/容错」配套。*
