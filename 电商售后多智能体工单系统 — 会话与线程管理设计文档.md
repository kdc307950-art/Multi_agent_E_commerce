## 一、概述

本文档定义电商售后多智能体工单系统的会话与线程管理方案。会话管理是 Agent 系统的核心基础设施，直接影响系统的**并发能力、租户/用户数据隔离性、故障恢复能力和可观测性**。本版本采用租户感知（tenant-aware）设计。

### 1.1 设计目标

| 目标 | 说明 |
| :-- | :-- |
| **状态持久化** | 对话状态可跨会话恢复，支持断点续聊 |
| **多租户隔离** | 不同租户、用户、会话的数据严格隔离，跨租户默认拒绝 |
| **高并发支持** | 支持数千并发会话，无状态冲突 |
| **故障恢复** | 系统崩溃后可从中断点恢复 |
| **可观测性** | 每个会话全链路可追踪、可审计 |
| **时间旅行** | 支持回放任意历史状态（调试/审计） |
| **租户生命周期** | 停用立即阻断请求；导出/删除按租户范围执行并可审计 |

### 1.2 核心概念

| 概念 | 说明 |
| :-- | :-- |
| **thread_id** | LangGraph 中唯一标识一个对话线程的 ID（不透明） |
| **Checkpoint** | LangGraph 在每次节点执行后保存的状态快照 |
| **Session** | 应用层概念，一个用户的一次完整对话周期 |
| **Conversation** | 用户与系统之间的消息交换序列 |
| **approval_id** | 审批单唯一标识，与 thread 解耦，用于安全审批 |
| **operation_id** | 业务操作唯一标识（幂等键） |
| **tenant_id** | 服务端认证上下文解析出的租户唯一标识，是所有业务数据面的最小隔离边界 |
| **TenantContext** | 经认证主体、租户成员关系、租户状态和角色校验后的不可变请求上下文 |

**关系**：一个 `Session` 对应一个独立的 `thread_id`，`thread_id` 背后是一系列 `Checkpoint` 组成的状态链。`Session`、`Checkpoint`、审批和操作都绑定同一个 `tenant_id`；审批/操作以 `approval_id`/`operation_id` 独立标识，不依赖 `thread_id` 内容推断归属。

### 1.3 租户模型与隔离策略

- **租户（tenant）**是订单、工单、会话、记忆、知识库、审批、审计和异步任务的最小隔离边界。
- **成员关系**存储在服务端 `tenant_memberships` 表；同一用户可属于多个租户，但每次请求只能激活一个租户上下文。租户成员角色仅为 `customer`、`agent`、`admin`、`approver`；`platform_admin` 属于独立的平台级授权，不是任何租户的成员角色。
- `tenant_id` 只从已验证的令牌声明/服务端租户选择流程解析，并再次校验成员状态与租户 `active` 状态；不接受 body、query、普通 Header 或 `thread_id` 中的租户值。
- 原型采用共享 PostgreSQL + 行级安全（RLS）/等效 repository guard；高合规租户可演进到独立 schema 或独立数据库，但应用契约保持一致。
- 所有缓存键、队列 payload、Milvus collection/partition、Neo4j 实体命名空间和 Langfuse metadata 都必须包含 `tenant_id`。

## 二、thread_id 生成策略

### 2.1 设计原则

- **全局唯一性**：任何两个会话的 `thread_id` 不能冲突
- **不可猜测**：使用高熵随机值，防止恶意遍历（安全考虑）
- **不嵌入身份**：**不在 `thread_id` 中编码 `user_id`**，避免泄露/伪造
- **归属独立**：用户与会话的归属关系由服务端 `sessions` 表维护，鉴权以服务端认证身份为准

### 2.2 推荐格式

使用**不透明随机 UUID**（推荐）：

```python
import uuid


def generate_thread_id() -> str:
    """生成不透明、不可猜测的 thread_id（不嵌入 user_id）。"""
    return str(uuid.uuid4())
```

### 2.3 前端传入 vs 后端生成

| 方式 | 适用场景 | 说明 |
| :-- | :-- | :-- |
| **前端生成** | 无状态前端、离线优先 | 前端生成后传给后端，可靠性较低 |
| **后端生成** | **推荐** | 后端统一管理，保证唯一性和合法性 |
| **混合模式** | 需要恢复历史会话 | 新会话由后端生成，历史会话由前端传递已知 ID |

**本项目采用方案：后端生成**。创建接口只接收认证主体，不接收 `tenant_id`；租户由 `TenantContext` 解析。

```python
# src/api/routes.py
from datetime import datetime, timedelta, timezone
from fastapi import APIRouter, Depends


@router.post("/api/sessions", response_model=NewSessionResponse)
async def create_session(ctx: TenantContext = Depends(get_tenant_context)):
    """服务端认证取 user/tenant，生成不透明 thread_id。"""
    thread_id = generate_thread_id()
    now = datetime.now(timezone.utc)
    expires_at = now + timedelta(days=7)
    # sessions 与 checkpoint scope 必须同一事务创建；任一失败则整体回滚。
    async with tenant_transaction(ctx) as tx:
        await session_store.create({
            "tenant_id": ctx.tenant_id,
            "thread_id": thread_id,
            "user_id": ctx.user_id,
            "created_at": now,
            "expires_at": expires_at,
            "status": "active",
        }, tx=tx)
        await checkpoint_scope_repo.create_active(
            tenant_id=ctx.tenant_id,
            thread_id=thread_id,
            expires_at=expires_at,
            tx=tx,
        )
    return NewSessionResponse(thread_id=thread_id)
```

`get_tenant_context` 必须在任何 graph/tool 调用前完成认证、成员关系、租户状态和角色校验；解析失败、租户停用或成员撤销统一拒绝，不回退到默认租户。

## 三、检查点 (Checkpoint) 配置

### 3.1 Checkpointer 选择

| Checkpointer | 存储 | 同步/异步 | 适用场景 |
| :-- | :-- | :-- | :-- |
| `InMemorySaver` | 内存 | 同步 | ❌ 仅本地开发测试 |
| `SqliteSaver` | SQLite 文件 | 同步 | ✅ 单机、小规模、嵌入式（原型） |
| `PostgresSaver` | PostgreSQL | 同步 | ⚠️ 同步服务可选；不是本项目异步网关的生产实现 |
| `AsyncPostgresSaver` | PostgreSQL | 异步 | ✅ 高并发异步服务 |
| `RedisSaver` | Redis | 异步 | ⚠️ 极致低延迟可选 |

### 3.2 本项目推荐：原型 SqliteSaver，生产 TenantScopedCheckpointer（基于 AsyncPostgresSaver）

**选择理由**：
1. PostgreSQL 可与其他业务数据（订单、用户）共用
2. `AsyncPostgresSaver` 是异步 FastAPI 运行面候选基线；它本身不提供租户过滤
3. 图编译时传入的必须是完整的 `TenantScopedCheckpointer`，使读、写、恢复和删除都无法绕过租户作用域
4. 最终可用性取决于锁定版本的接口、RLS 连接闭环和预发布验收；当前仍是设计态

**初始化代码**：

```python
# src/infrastructure/checkpointer.py
import os
from contextlib import asynccontextmanager
from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver

DB_URI = os.getenv("DATABASE_URL", "postgresql://user:pass@localhost:5432/langgraph")


@asynccontextmanager
async def get_raw_async_checkpointer():
    """仅供 TenantScopedCheckpointer 内部组装，禁止交给业务代码或 graph.compile。"""
    async with AsyncPostgresSaver.from_conn_string(DB_URI) as saver:
        # 首次部署是否需要 setup/assetup 取决于实际安装版本；
        # 必须按该版本公开 API 执行一次性建表并记录验收结果。
        yield saver
```

> **版本边界**：上例采用候选基线 `langgraph==1.2.11` 与
> `langgraph-checkpoint-postgres==3.1.2` 的上下文管理写法。由于当前文档环境
> 尚未安装 Python 3.12 和这些依赖，`from_conn_string`、一次性建表方法
> （`setup`/`asetup` 或版本对应名称）必须在安装验收时以实际暴露的 API 为准，
> 不得仅凭文档示例宣称已验证。

### 3.3 Checkpoint 租户适配结构（PostgreSQL）

`AsyncPostgresSaver` 的内部表结构和主键由所锁定的 LangGraph 版本决定，不能把自定义 `tenant_id` 列假定为官方 checkpointer 已支持的字段。本项目采用“**官方 checkpoint 表 + 应用层权威映射表 + 完整保存器适配层**”模式：官方 saver 负责状态序列化；`TenantScopedCheckpointer` 必须实现 LangGraph 所需的完整异步保存器接口，并在每次访问时强制校验可信的请求作用域。

```sql
CREATE TABLE checkpoint_thread_scopes (
    tenant_id TEXT NOT NULL REFERENCES tenants(id),
    thread_id TEXT NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    last_active_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
    status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'deleting')),
    cleanup_attempts INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (tenant_id, thread_id),
    UNIQUE (thread_id)
);

CREATE INDEX idx_checkpoint_scope_expiry
    ON checkpoint_thread_scopes (tenant_id, expires_at);
```

```python
from contextvars import ContextVar
from dataclasses import dataclass
from langgraph.checkpoint.base import BaseCheckpointSaver


@dataclass(frozen=True)
class CheckpointRequestScope:
    tenant_id: str
    user_id: str
    thread_id: str
    source: str  # request / approval_resume / cleanup / tenant_delete


current_checkpoint_scope: ContextVar[CheckpointRequestScope] = ContextVar(
    "current_checkpoint_scope"
)


class TenantScopedCheckpointer(BaseCheckpointSaver):
    """设计接口骨架：实际实现须按锁定 LangGraph 版本完成并通过兼容性测试。"""

    # graph.compile(checkpointer=...) 只接收此对象；不得把 raw saver 暴露出去。
    # 每个方法都从 ContextVar 读取服务端建立的可信 scope，随后校验 config/thread_id。
    async def aget_tuple(self, config): ...
    async def alist(self, config, *, filter=None, before=None, limit=None): ...
    async def aput(self, config, checkpoint, metadata, new_versions): ...
    async def aput_writes(self, config, writes, task_id, task_path=""): ...
    async def adelete_thread(self, thread_id): ...
```

**不可省略的实现约束**：

1. **完整接口，而非 ctx 包装器**：适配器必须覆盖锁定版本实际调用的 `aget_tuple`、`alist`、`aput`、`aput_writes`、`adelete_thread` 及其同步/异步等价接口；方法签名以安装后的 `BaseCheckpointSaver` 为准。不得设计额外 `ctx` 形参让图内部无法传入，也不得只包装 `aget_state`。
2. **可信上下文与编译入口**：API/审批恢复/后台清理在调用图前，先通过 `checkpoint_request_scope(ctx, thread_id)` 验证 session + scope 归属，再把不可变 `CheckpointRequestScope` 写入 `ContextVar`；适配器从该上下文获取租户，绝不把 `config.configurable.tenant_id` 当作授权依据。上下文缺失、thread 不一致、scope 非 `active` 一律拒绝并审计。
3. **同连接 RLS 闭环**：适配器须在**实际执行 saver SQL 的同一连接和同一事务**内，以参数化 `set_config('app.tenant_id', verified_tenant, true)` 设置作用域，再访问官方 checkpoint 表。禁止在业务连接先 `SET LOCAL`、随后让 `AsyncPostgresSaver` 自行新开/借用另一连接；那样 RLS 不生效。实现可以是经锁定版本验收的 saver 子类/受控 fork，或完整自定义 saver；两者都必须实现完整接口。
4. **唯一入口**：`builder.compile(checkpointer=tenant_scoped_checkpointer)` 是图、恢复、历史和审批续跑的唯一 checkpoint 入口；业务代码、任务和调试工具均不得持有 raw `AsyncPostgresSaver`。后台删除使用显式 `source` 和目标 `tenant_id` 的受控系统上下文，不存在默认租户。
5. **RLS 与运行角色**：`checkpoint_thread_scopes` 和锁定版本实际创建的 checkpoint 表均启用 `ENABLE` + `FORCE ROW LEVEL SECURITY`。运行时数据库角色必须 `NOBYPASSRLS` 且不是超级用户；迁移/备份角色与应用运行角色分离。官方表名、列和 policy 在安装 `langgraph-checkpoint-postgres==3.1.2` 后核对，不得照抄未验收的 schema。

### 3.4 Checkpoint 生命周期管理

保留期采用**最后一次被接受的会话活动后滑动 7 天**。每次 `start`、审批续跑或合法历史恢复都在同一受控事务内更新 `sessions.last_active_at / expires_at` 与 `checkpoint_thread_scopes.last_active_at / expires_at`；断线重连只续传事件，不自动延长 TTL。

```python
async def cleanup_expired_threads(batch_size: int = 100) -> None:
    """设计伪代码：先占用、再删除保存器数据、最后删除 scope/session；全程可重试。"""
    for candidate in await scope_repo.claim_expired_for_cleanup(
        now=datetime.now(timezone.utc), batch_size=batch_size
    ):
        # claim 使用 FOR UPDATE SKIP LOCKED，将 active 标记为 deleting，阻断新的图执行。
        try:
            async with checkpoint_request_scope(candidate.system_ctx, candidate.thread_id,
                                                source="cleanup"):
                await tenant_scoped_checkpointer.adelete_thread(candidate.thread_id)
            # 只有 adelete_thread 成功后，才在事务内删除 scope 与 session，并写审计事件。
            await scope_repo.finish_cleanup(candidate, delete_session=True)
        except Exception as exc:
            # 保留 deleting scope、递增尝试次数、写审计/告警；下一轮幂等重试。
            await scope_repo.record_cleanup_failure(candidate, exc)
```

`adelete_thread` 必须覆盖锁定版本中该 thread 的 checkpoint、blob 与 write 记录；仅删除 scope 映射不等于删除 checkpoint。租户删除采用相同的“先停用租户 → 对全部 scope claim → `adelete_thread` 成功 → 删除 scope/session → 清理其他数据面”工作流，并为每个阶段写入可恢复的删除任务与审计记录。不得因为一次失败先删 scope 或 session；否则既遗留状态，又失去安全定位依据。

## 四、LangGraph 图执行与 thread_id 绑定

```python
# src/service/agent_service.py
async def process_message(ctx: TenantContext, thread_id: str, user_message: str) -> str:
    await validate_session_owner(thread_id, ctx)
    config = {"configurable": {"thread_id": thread_id, "tenant_id": ctx.tenant_id}}
    input_state = {"messages": [{"role": "user", "content": user_message}],
                   "tenant_id": ctx.tenant_id, "user_id": ctx.user_id}
    async with checkpoint_request_scope(ctx, thread_id, source="request"):
        result = await compiled_graph.ainvoke(input_state, config=config)
    return result.get("final_response") or result["messages"][-1]["content"]
```

## 五、多租户与多用户会话隔离

### 5.1 隔离原则

1. **租户隔离**：任何资源查询必须同时匹配 `tenant_id` 与资源 ID；不同租户即使资源 ID 相同也不可见。
2. **用户隔离**：客户仅能访问本人会话；客服/管理员按本租户 RBAC 访问必要资源；平台运维只能使用独立的 `PlatformOperationContext` 和显式 `platform_admin` 能力，不能借用租户会话接口。
3. **权限校验**：在 API 入口校验认证主体、租户成员状态、租户状态、角色以及 `thread_id` 的租户/用户归属。
4. **安全设计**：`thread_id` 为不透明随机值，**不解析其内容做鉴权**；归属由服务端 `sessions` 表和 RLS/等效 guard 消除伪造风险。
5. **默认拒绝**：缺少租户上下文、租户停用、成员撤销、资源不存在或跨租户访问统一拒绝并记录安全审计事件。

### 5.2 会话元数据存储

```sql
CREATE TABLE tenants (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    status TEXT NOT NULL CHECK (status IN ('active', 'suspended', 'deleting')),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE tenant_memberships (
    tenant_id TEXT NOT NULL REFERENCES tenants(id),
    user_id TEXT NOT NULL,
    role TEXT NOT NULL CHECK (role IN ('customer', 'agent', 'admin', 'approver')),
    status TEXT NOT NULL CHECK (status IN ('active', 'revoked')),
    PRIMARY KEY (tenant_id, user_id)
);

CREATE TABLE platform_role_assignments (
    user_id TEXT NOT NULL,
    capability TEXT NOT NULL CHECK (capability IN ('platform_admin')),
    status TEXT NOT NULL CHECK (status IN ('active', 'revoked')),
    granted_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    PRIMARY KEY (user_id, capability)
);

CREATE TABLE sessions (
    id SERIAL PRIMARY KEY,
    tenant_id TEXT NOT NULL REFERENCES tenants(id),
    user_id TEXT NOT NULL,
    thread_id TEXT NOT NULL,
    title TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    last_active_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
    status TEXT DEFAULT 'active',
    message_count INTEGER DEFAULT 0,
    total_tokens_used INTEGER DEFAULT 0
);

CREATE UNIQUE INDEX uq_sessions_tenant_thread ON sessions (tenant_id, thread_id);
CREATE INDEX idx_sessions_tenant_user ON sessions (tenant_id, user_id);
CREATE INDEX idx_sessions_tenant_thread ON sessions (tenant_id, thread_id);
CREATE INDEX idx_sessions_tenant_expiry ON sessions (tenant_id, expires_at);
```

业务表（`orders`、`tickets`、`approvals`、`refund_ops` 等）必须采用同样的 `tenant_id` 外键和联合唯一约束，例如 `UNIQUE (tenant_id, order_id)`、`UNIQUE (tenant_id, idempotency_key)`。推荐在事务入口设置 `SET LOCAL app.tenant_id = '<verified tenant>'`，RLS policy 只允许读取该值对应的行。

`platform_role_assignments` 不能参与普通租户请求的 `TenantContext` 解析。平台运维/合规接口必须另行建立 `PlatformOperationContext`，显式记录目标租户、操作目的、最小授权范围、二次确认和审计 ID；该上下文不得用于 `/api/chat`、审批或任何 `execute_*` 路径。`sessions` 与 `checkpoint_thread_scopes` 的创建、滑动续期、过期标记和最终删除必须以同一 `tenant_id + thread_id` 在受控事务中推进；scope 进入 `deleting` 后，任何新请求、审批续跑和历史恢复都必须拒绝。

### 5.3 API 层权限校验

```python
# src/api/dependencies.py
from fastapi import Depends, HTTPException


async def validate_session_owner(thread_id: str, ctx: TenantContext) -> Session:
    """同时校验租户、成员、角色和用户归属；不信任客户端 tenant/user。"""
    session = await session_store.get_by_thread_id(
        tenant_id=ctx.tenant_id, thread_id=thread_id
    )
    if not session:
        raise HTTPException(status_code=404, detail="会话不存在")
    if session["tenant_id"] != ctx.tenant_id:
        raise HTTPException(status_code=403, detail="无权访问该会话")
    if session["user_id"] != ctx.user_id and ctx.role not in {"agent", "admin", "approver"}:
        raise HTTPException(status_code=403, detail="无权访问该会话")
    return session
```

### 5.4 并发安全

- **乐观锁**：LangGraph 使用 `checkpoint_id` 链式结构，更新时检查父节点
- **避免同一 thread_id 并发写**：应用层可通过 Redis 分布式锁防止同一会话同时被多个请求修改

```python
from redis.asyncio import Redis


async def lock_thread_id(tenant_id: str, thread_id: str, ttl: int = 30) -> bool:
    redis = Redis.from_url("redis://localhost:6379")
    return await redis.set(f"lock:{tenant_id}:{thread_id}", "1", nx=True, ex=ttl)
```

## 六、可观测性

### 6.1 自托管 Langfuse 集成

```python
config = {
    "configurable": {"thread_id": thread_id, "tenant_id": ctx.tenant_id},
    "metadata": {
        "tenant_id": ctx.tenant_id,
        "user_id": ctx.user_id,
        "session_id": thread_id,
        "environment": os.getenv("ENV", "development"),
    }
}
result = await graph.ainvoke(input_state, config=config)
```

> 默认接入**自托管 Langfuse**，数据只在项目方控制的服务器、网络和备份边界内流转。若改用云端 LangSmith，需在部署备注中明确数据流、合规影响和“不再满足完全自托管”的变化。

### 6.2 自定义指标采集

```python
from prometheus_client import Counter, Histogram

session_counter = Counter("sessions_total", "总会话数", ["status"])
message_counter = Counter("messages_total", "总消息数", ["intent"])
latency_histogram = Histogram("request_latency_seconds", "请求延迟")
```

> `tenant_id` 写入 Langfuse trace、审计事件和受控指标聚合表；不要把无限增长的租户 ID 作为 Prometheus 标签，以免高基数导致监控系统退化。

### 6.3 会话审计日志

```python
from datetime import datetime, timezone
import structlog

logger = structlog.get_logger()


async def audit_log(action: str, ctx: TenantContext, thread_id: str, details: dict):
    logger.info(
        "audit",
        action=action,
        tenant_id=ctx.tenant_id,
        user_id=ctx.user_id,
        thread_id=thread_id,
        details=details,
        timestamp=datetime.now(timezone.utc).isoformat(),
    )
```

## 七、故障恢复与时间旅行

### 7.1 从 Checkpoint 恢复

```python
async with checkpoint_request_scope(ctx, thread_id, source="history"):
    async for snapshot in compiled_graph.aget_state_history(config):
        history.append({
            "checkpoint_id": snapshot.config["configurable"]["checkpoint_id"],
            "values": snapshot.values,
            "next": snapshot.next,
            "created_at": snapshot.metadata.get("created_at"),
        })
```

### 7.2 断点续聊

```python
@router.get("/api/sessions/{thread_id}/messages")
async def get_session_messages(thread_id: str, ctx: TenantContext = Depends(get_tenant_context)):
    """获取会话消息历史；先做租户/用户归属校验，再由受控 saver 读取。"""
    await validate_session_owner(thread_id, ctx)
    config = {"configurable": {"thread_id": thread_id, "tenant_id": ctx.tenant_id}}
    async with checkpoint_request_scope(ctx, thread_id, source="history"):
        state = await compiled_graph.aget_state(config=config)
    return {"messages": state.values.get("messages", [])}
```

### 7.3 超时与过期清理

```python
async def expire_inactive_sessions(tenant_id: str, inactive_days: int = 7):
    # 仅标记/排队，不得绕过 cleanup_expired_threads 直接删除 scope 或 session。
    cutoff = datetime.now(timezone.utc) - timedelta(days=inactive_days)
    await session_store.mark_expired_before(tenant_id, cutoff)
    await cleanup_expired_threads()
```

> 统一策略：检查点与会话均保留 **7 天**，避免 7/30 天口径冲突。

## 八、性能优化策略

| 优化点 | 方法 | 说明 |
| :-- | :-- | :-- |
| **连接池** | PostgreSQL 连接池 | `psycopg_pool` 或 `asyncpg` 连接池 |
| **Checkpoint 压缩** | 定期合并 | 长对话可压缩历史 Checkpoint 减少存储 |
| **缓存最新状态** | Redis 缓存 | 高频访问的 `tenant_id:thread_id` 状态缓存；禁止无租户 key |
| **异步执行** | 使用 `ainvoke` | FastAPI 中全异步执行 |
| **索引优化** | 数据库索引 | `(tenant_id, thread_id)`、`(tenant_id, created_at)` 建索引 |
| **批量清理** | 定时任务 | 夜间清理过期 Checkpoint |

## 九、项目实施清单

| 序号 | 任务 | 说明 | 状态 |
| :-- | :-- | :-- | :-- |
| 1 | 设计 `thread_id` 生成函数 | 不透明 UUID，不嵌入 user_id | 待开发 |
| 2 | 部署 PostgreSQL | 本地开发可用 Docker | 待部署 |
| 3 | 初始化 raw `AsyncPostgresSaver` | 按锁定版本使用 `from_conn_string`；首次部署按实际 API 执行一次性建表（`setup`/`asetup` 等）并记录结果 | 待开发 |
| 4 | 实现并验收完整 `TenantScopedCheckpointer` | 覆盖 `aget_tuple/alist/aput/aput_writes/adelete_thread` 与实际版本等价接口；raw saver 不暴露 | 待开发 |
| 5 | 图编译传入受控 Checkpointer | `builder.compile(checkpointer=tenant_scoped_checkpointer)`；invoke/恢复/历史/审批续跑都建立可信 ContextVar 作用域 | 待开发 |
| 6 | 创建会话与 scope 元数据表 | `sessions` + `checkpoint_thread_scopes` 原子创建、滑动 7 天 TTL、RLS/角色边界 | 待开发 |
| 7 | 实现会话创建 API | `POST /api/sessions`，服务端鉴权 | 待开发 |
| 8 | 实现会话权限校验 | 中间件或依赖注入 | 待开发 |
| 9 | 集成自托管 Langfuse | 配置 `LANGFUSE_HOST` | 待配置 |
| 10 | 实现会话历史查询 | `GET /api/sessions/{thread_id}/messages` | 待开发 |
| 11 | 实现过期/租户删除任务 | claim → `adelete_thread` → 删除 scope/session；失败可重试、全程审计 | 待开发 |
| 12 | 实现分布式锁 | 防止同一会话并发写 | 待开发 |
| 13 | 压测与性能调优 | 检查点读写性能 | 待测试 |
| 14 | 租户上下文中间件 | 认证主体 + 成员关系 + 租户状态 + RBAC | 待开发 |
| 15 | 业务表租户迁移 | `tenant_id` 外键、联合唯一约束、RLS policy | 待开发 |
| 16 | 数据面隔离 | Milvus、Neo4j、Redis、Celery、Langfuse 全部带租户范围 | 待开发 |
| 17 | 多租户安全验收 | 同租户允许、跨租户/停用租户拒绝、无串租户；完整 saver/RLS/删除路径均覆盖 | 待测试 |
